Взаимодействие этих структур — ключ к пониманию архитектуры StarRocks. Давайте разберем это наглядно, от логического уровня до физического хранения.

### 🗺️ Логическая и физическая иерархия данных в StarRocks

Представьте себе эту структуру как матрешку:

**Логический Уровень (Что видит пользователь)**
`Таблица (Table)` → `Партиция (Partition)` → `Бакет (Bucket)`

**Физический Уровень (Как данные лежат на диске)**
`Бакет (Bucket)` → `Таблет (Tablet)` → `Строки/Колонки` → `Сегмент (Segment)` → `Страница (Page)`

---

### 📊 Наглядная схема отношений

```
      [ Логическая Таблица ]
              |
       (PARTITION BY ...)
              |
    +---------v---------+
    |   Партиция_1      |    Партиция_2     ...
    | (напр., p202301)  |  (напр., p202302)
    +---------|---------+
              |
       (DISTRIBUTED BY HASH(...))
              |
    +---------v-------------------+
    |  Бакет (Bucket)             |
    |  [Виден пользователю как]   |
    +---------|-------------------+
              |
              v  Физически реализуется как
    +---------|-------------------+
    |  Таблет (Tablet)            |  <--- Основная единица репликации и перемещения
    |  [Физический объект]        |
    +---------|-------------------+
              |
       +------v------+  +-----...-----+
       |   Реплика   |  |   Реплика   |  (обычно 3 реплики)
       |   Таблета   |  |   Таблета   |
       +------|------+  +-----|-------+
              |               |
       Хранится на        Хранится на
     [BE-Node A]        [BE-Node B]
```

---

### 🔎 Детальное описание каждого компонента

**1. BE-Node (Backend Node)**
*   **Что это:** Физический сервер (или контейнер/виртуальная машина), где **фактически хранятся данные** и выполняются вычислительные задачи.
*   **Аналог:** Рабочая лошадка в кластере.
*   **Роль:** Хранит реплики таблетов, обрабатывает запросы (сканирование, агрегацию), получаемые от FE.

**2. Partition (Партиция)**
*   **Что это:** **Логическое разделение таблицы по диапазону значений** (обычно по дате). Создается через `PARTITION BY RANGE(...)`.
*   **Цель:**
    *   **Pruning (Отсечение):** Запрос, фильтрующий по ключу партиции (например, `dt = '2024-01-01'`), касается только нужных партиций, а не всей таблицы.
    *   **Управление жизненным циклом:** Удобно удалять старые данные (`DROP PARTITION`) или управлять TTL.
*   **Пример:** `PARTITION BY RANGE(dt) (START ("2024-01-01") END ("2024-02-01") EVERY (INTERVAL 1 DAY))`

**3. Bucket (Бакет) / Tablet (Таблет)**
*   **Это самая важная связка.**
*   **Bucket — это логическое понятие.** Когда вы пишете `DISTRIBUTED BY HASH(user_id) INTO 10 BUCKETS`, вы говорите: "Разбей мои данные на 10 логических групп".
*   **Tablet — это физическая реализация Bucket.** Каждому логическому бакету соответствует **один физический таблет**. Это минимальная единица горизонтального масштабирования, репликации и балансировки.
*   **Цель:**
    *   **Распределение данных:** Хеш-функция равномерно раскидывает данные по таблетам.
    *   **Локализация вычислений:** JOIN и агрегации стараются выполняться локально на BE, где лежит таблет, минимизируя сетевой обмен (Shuffle).
    *   **Параллелизм:** Один запрос может параллельно сканировать множество таблетов на разных BE.

**4. Segment (Сегмент) и Rowset (Набор строк)**
*   **Что это:** **Внутренняя структура хранения внутри одного Таблета.**
*   **Rowset:** Это набор данных, добавленных в таблет за одну операцию коммита (например, один бatch-инсерт). Версионность таблета реализована как последовательность Rowsets.
*   **Segment (Segment File):** Каждый Rowset при записи на диск разбивается на несколько **Columnar Segment Files**. Это и есть физические файлы в формате **columnar storage**, лежащие на диске BE.
    *   Данные внутри сегмента сортируются (на основе `ORDER BY` при создании таблицы).
    *   Сегмент содержит метаданные для эффективного "пропуска" данных (статистики min/max, индексы).

---

### 🎯 Итог: как это все работает вместе на примере

1.  **Вы создаете таблицу:**
    ```sql
    CREATE TABLE events (
        dt DATE,
        user_id INT,
        event_type VARCHAR
    )
    PARTITION BY RANGE(dt) (...)
    DISTRIBUTED BY HASH(user_id) INTO 10 BUCKETS;
    ```

2.  **StarRocks создает структуру:**
    *   Для каждой партиции создается **10 логических бакетов**.
    *   Каждому бакету ставится в соответствие **1 физический таблет**. Итого: `N_партиций * 10` таблетов.
    *   Каждый таблет реплицируется (например, в 3 копии). Итого: `(N_партиций * 10) * 3` реплик таблетов.
    *   Эти реплики равномерно распределяются по всем **BE-Node** в кластере.

3.  **Вы вставляете данные:**
    *   Данные партиционируются по `dt`.
    *   Внутри партиции данные распределяются по 10 таблетам с помощью хеша от `user_id`.
    *   На каждом BE для каждого таблета создается новый **Rowset**, который записывается на диск как **Segment File**.

4.  **Вы выполняете запрос:**
    ```sql
    SELECT user_id, COUNT(*) FROM events
    WHERE dt = '2024-01-15' AND user_id = 123;
    ```
    *   **FE** принимает запрос, определяет, что нужна только одна партиция `p20240115`.
    *   По префиксному индексу и хешу от `user_id=123` определяется, что данные лежат в конкретном таблете (например, `Bucket 3 / Tablet_15_3`).
    *   **FE** направляет запрос на тот **BE-Node**, где находится лидер-реплика этого таблета.
    *   **BE** открывает соответствующие **Segment Files** этого таблета, использует статистику и индексы для быстрого поиска нужных строк и выполняет агрегацию.

Таким образом, **BE-Node** — это физический хост, **Partition** и **Bucket** — логические единицы организации, **Tablet** — ключевая физическая единица управления, а **Segment** — внутренняя единица columnar-хранения внутри Tablet.
