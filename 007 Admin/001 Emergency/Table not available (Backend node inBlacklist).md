> после рестарта машины и старта последовательно fe и be при попытке доступа к таблицам пишет  
> SQL Error [1064] [42000]: Backend node not found. Check if any backend node is down.backend: [sl11-demostand-sr-01 alive: true inBlacklist: true] 

Ошибка `inBlacklist: true` означает, что backend (BE) сервер StarRocks находится в черном списке, и FE (frontend) не направляет к нему запросы. Это происходит при нештатном завершении работы BE (например, при внезапной перезагрузке машины).

## 🔧 Решение проблемы

### 1. **Проверьте состояние всех BE-узлов**
```sql
SHOW PROC '/backends'\G
```
Посмотрите на столбцы `Alive` (должно быть `true`), `SystemDecommissioned` и `TabletNum`.

### 2. **Снимите BE с черного списка (декомиссии)**

Самый прямой способ — отключить режим декомиссии для проблемного BE:

```sql
CANCEL DECOMMISSION BACKEND "sl11-demostand-sr-01:9050";
```
Замените `9050` на правильный heartbeat порт вашего BE (по умолчанию это `9050`).

### 3. **Если не помогло, попробуйте полную перерегистрацию**
Сначала добавьте BE заново (после удаления):
```sql
-- Удаляем BE (осторожно, убедитесь, что это единственный BE!)
ALTER SYSTEM DROP BACKEND "sl11-demostand-sr-01:9050";

-- Добавляем его снова
ALTER SYSTEM ADD BACKEND "sl11-demostand-sr-01:9050";
```

### 4. **Проверьте наличие таблиц на BE**
```sql
-- Найдите tablet'ы, привязанные к проблемному BE
SHOW PROC '/tablet_health';
```
Если tablet'ы есть только на этом BE, сначала нужно восстановить репликацию.

## ⚙️ Профилактика на будущее

### 1. **Всегда останавливайте StarRocks корректно**
```bash
# На каждом BE
./bin/stop_be.sh
# На каждом FE  
./bin/stop_fe.sh
```

### 2. **Настройте автоматическое восстановление**
В конфиге BE (`be.conf`) убедитесь, что параметры восстановления включены:
```ini
enable_debug_actions = false
tablet_checker_interval_seconds = 300
```

## 💡 Если проблема не решается

1. **Проверьте логи BE** (`log/be.INFO`):
   ```bash
   tail -100 log/be.INFO | grep -i "blacklist\|heartbeat"
   ```

2. **Убедитесь, что все порты открыты**:
   ```bash
   netstat -tlnp | grep 9050
   ```

3. **Перезапустите BE после изменений**:
   ```bash
   ./bin/stop_be.sh && ./bin/start_be.sh --daemon
   ```

После выполнения `CANCEL DECOMMISSION` статус `inBlacklist` должен измениться на `false` в течение нескольких минут.
