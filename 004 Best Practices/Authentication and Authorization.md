# [Authentication and Authorization](https://docs.starrocks.io/docs/best_practices/authentication_authorization/)

```
разобраться с системой идентификации и авторизации
хочется четкой систематизации для начального понимания
рекомендуется использование внешних систем, а я привык к IBAC/RBAC назначениям
почему такой подход плох или неудобен?
ведь там вся информация содержится внутри субд, и мы  не зависим ни от каких внешних систем
работает субд - работает идентификация и авторизация
а при внешней системе ее падение вызовет невозможность работы с базой
чем это удобнее?
и как внешняя система дает разрешение на выполнение операций?
через плагин?
т.е. права (на что?) мы как-то раздаем в наружной системе, а она через встраиваемый плагин разрешает или не разрешает пользователю выполнять запросы и получать доступ к таблицам (строкам? колонкам?)?
```
[Manage permissions with Apache Ranger](https://docs.celerdata.com/BYOC/docs/administration/user_privs/Column_and_row-level_security/ranger_plugin/)

### 🎯 Ключевые концепции: Аутентификация, Авторизация и Провайдеры

StarRocks разделяет процессы проверки личности и проверки прав, используя модульную систему:

*   **Аутентификация (Authentication)** — отвечает на вопрос «**Кто ты?**». Система проверяет вашу личность, например, с помощью логина и пароля, хранящегося в самой СУБД (нативная), или через внешние системы: LDAP, OAuth 2.0 / OIDC.
*   **Авторизация (Authorization)** — отвечает на вопрос «**Что тебе можно?**». Это система контроля доступа, которая проверяет ваши права на выполнение операций (SELECT, INSERT и т.д.). StarRocks поддерживает встроенную систему на основе ролей (RBAC) и интеграцию с **Apache Ranger** для централизованного управления политиками.
*   **Провайдер групп (Group Provider)** — это связующее звено. Он получает информацию о группах, к которым принадлежит пользователь, из внешних систем (например, из LDAP или операционной системы), а встроенная система RBAC или Apache Ranger могут назначать права уже на основе этой групповой принадлежности.

### 🤔 Почему рассматривают внешние системы?

Ваши опасения по поводу надежности внутренней системы (IBAC/RBAC) абсолютно логичны. Однако внешние системы аутентификации и авторизации предлагают решения для проблем, которые возникают в больших организациях.

Сравним два подхода в контексте крупного предприятия:

| Аспект | Внутренняя система StarRocks (IBAC/RBAC) | Внешние системы (LDAP, Apache Ranger) |
| :--- | :--- | :--- |
| **Управление пользователями** | Ручное создание и удаление аккаунтов для каждого сотрудника в каждой СУБД. Трудоемко, высок риск ошибки. | Единый центр управления учетными записями и группами. При увольнении доступ отзывается во всех системах сразу. |
| **Безопасность** | Пароли и права хранятся в СУБД. | Снижение количества учетных данных, единые стандарты безопасности. **Ликвидация «суперпользователей»** в СУБД, когда запросы выполняются от его лица. |
| **Сложность администрирования** | Проста для одной СУБД. | Сложность переносится на настройку и поддержку внешних систем. |
| **Надежность** | Зависит от одной СУБД. | Создает единую точку отказа. |
| **Сценарий применения** | Небольшие команды, проекты с простой структурой прав. | Крупные предприятия со сложной структурой, множеством сотрудников и строгими требованиями к аудиту. |

### 🔧 Как работает внешняя авторизация на примере Apache Ranger

Вы правильно поняли принцип «доверенного посредника». Вот как это выглядит на практике с Apache Ranger:

1.  **Настройка**: В кластере StarRocks настраивается плагин, который знает адрес сервера **Apache Ranger**.
2.  **Создание политик**: Администратор в удобном веб-интерфейсе Ranger создает политики вида: «**Группа `finance_analysts` имеет право `SELECT` на таблицу `sensitive_data`**». Политики могут быть очень детализированными, вплоть до уровня **столбцов и строк**.
3.  **Выполнение запроса**:
    *   Пользователь отправляет SQL-запрос в StarRocks.
    *   StarRocks определяет, какие права нужны для его выполнения.
    *   StarRocks через плагин отправляет запрос в Apache Ranger: «Пользователь X, принадлежащий к группам Y и Z, хочет выполнить действие A с объектом B. Разрешить?».
    *   **Apache Ranger** проверяет свои политики и возвращает решение: **«allow»** или **«deny»**.
    *   StarRocks выполняет запрос или возвращает ошибку доступа.

### 💡 Резюме и рекомендации

*   **Подход StarRocks** не является отказом от RBAC. Это **расширение**, которое позволяет гибко выбирать между простой встроенной моделью и мощными внешними системами, вплоть до использования **каталожно-ориентированного контроля доступа**, как в Apache Iceberg.
*   **Ваш подход с внутренним RBAC идеален**, если вы работаете в небольшой команде и ваша инфраструктура не слишком сложна. Это надежно и просто.
*   **Внешние системы становятся необходимы**, когда масштаб и сложность управления создают больше проблем, чем гипотетический отказ самой системы аутентификации. Они решают проблемы операционного характера и усиления безопасности в распределенных средах.
