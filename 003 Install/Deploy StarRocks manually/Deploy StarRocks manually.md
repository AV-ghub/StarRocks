# Deploy StarRocks manually
## Невозможность создать таблицу в базе
```bash
SQL Error [1064] [42000]: Getting analyzing error. Detail message: Cluster has no available capacity.
  Getting analyzing error. Detail message: Cluster has no available capacity.
```
### Разбираемся с конфигурацией StarRocks
#### Ставим MySql
```
sudo apt update
sudo apt install default-mysql-client

mysql -h 127.0.0.1 -P 9030 -uroot
MySQL [(none)]> \q
```

Ошибка **"Cluster has no available capacity"** и отсутствие результатов при выполнении команд `SHOW BACKENDS` и `SHOW COMPUTE NODES` указывают на то, что в вашем кластере StarRocks нет работающих вычислительных узлов, которые могли бы обработать запрос на создание таблицы. 

### 🔍 Диагностика проблемы

Для начала точно определим, с какими узлами мы работаем. В зависимости от архитектуры вашего кластера (**Shared-nothing** или **Shared-data**), нужные узлы будут называться по-разному.

### 🛠️ Решение: Добавление узлов в кластер

Если при выполнении `SHOW BACKENDS` или `SHOW COMPUTE NODES` вы не видите строк с `Alive: true`, значит узлы не были автоматически зарегистрированы в FE (Frontend). 

**Добавьте узел в кластер:**
    *   **Если у вас есть BE-узел (Backend):**
        ```sql
        ALTER SYSTEM ADD BACKEND "be_host:heartbeat_port";
        ```
        Например: `ALTER SYSTEM ADD BACKEND "starrocks-be:9050";`
    *   **Если у вас есть CN-узел (Compute Node):**
        ```sql
        ALTER SYSTEM ADD COMPUTE NODE "cn_host:heartbeat_port";
        ```
### ✅ Проверка результата

После выполнения команды добавления проверьте статус узлов:
```sql
-- Для Backend-узлов
SHOW BACKENDS;

-- Для Compute-узлов  
SHOW COMPUTE NODES;
```
Убедитесь, что в колонке **`Alive`** отображается **`true`**. После этого ошибка "Cluster has no available capacity" при создании таблицы должна исчезнуть.














