# Deploy StarRocks manually
## Невозможность создать таблицу в базе
```bash
SQL Error [1064] [42000]: Getting analyzing error. Detail message: Cluster has no available capacity.
  Getting analyzing error. Detail message: Cluster has no available capacity.
```
### Разбираемся с конфигурацией StarRocks
#### Ставим MySql
```
sudo apt update
sudo apt install default-mysql-client

mysql -h 127.0.0.1 -P 9030 -uroot
MySQL [(none)]> \q
```

Ошибка **"Cluster has no available capacity"** и отсутствие результатов при выполнении команд `SHOW BACKENDS` и `SHOW COMPUTE NODES` указывают на то, что в вашем кластере StarRocks нет работающих вычислительных узлов, которые могли бы обработать запрос на создание таблицы. 

#### 🔍 Диагностика проблемы

Для начала точно определим, с какими узлами мы работаем. В зависимости от архитектуры вашего кластера (**Shared-nothing** или **Shared-data**), нужные узлы будут называться по-разному.

#### 🛠️ Решение: Добавление узлов в кластер

Если при выполнении `SHOW BACKENDS` или `SHOW COMPUTE NODES` вы не видите строк с `Alive: true`, значит узлы не были автоматически зарегистрированы в FE (Frontend). 

**Добавьте узел в кластер:**
    *   **Если у вас есть BE-узел (Backend):**
        ```sql
        ALTER SYSTEM ADD BACKEND "be_host:heartbeat_port";
        ```
        Например: `ALTER SYSTEM ADD BACKEND "starrocks-be:9050";`
    *   **Если у вас есть CN-узел (Compute Node):**
        ```sql
        ALTER SYSTEM ADD COMPUTE NODE "cn_host:heartbeat_port";
        ```
#### ✅ Проверка результата

После выполнения команды добавления проверьте статус узлов:
```sql
-- Для Backend-узлов
SHOW BACKENDS;

-- Для Compute-узлов  
SHOW COMPUTE NODES;
```
Убедитесь, что в колонке **`Alive`** отображается **`true`**. После этого ошибка "Cluster has no available capacity" при создании таблицы должна исчезнуть.

### Несоответствие узлов
```
SQL Error [1064] [42000]: Table replication num should be less than or equal to the number of available backends. You can change this default by setting the replication_num table properties. Current available backends: [10043], table=crashdata, replication_num=3, default_replication_num=3
  Table replication num should be less than or equal to the number of available backends. You can change this default by setting the replication_num table properties. Current available backends: [10043], table=crashdata, replication_num=3, default_replication_num=3
```
Ошибка возникает из-за того, что StarRocks по умолчанию пытается создать три копии данных (реплики) для отказоустойчивости, но в кластере только одна нода BE. Решить это можно двумя способами: изменить настройки репликации для всей системы или для конкретной таблицы.

#### 💡 Решение 1: Установить системное значение по умолчанию

Самый простой способ — уменьшить количество реплик по умолчанию для всей системы. Для этого нужно изменить конфигурационный файл FE.

1.  **Откройте файл `fe.conf`**. Обычно он находится по пути `/opt/starrocks/fe/conf/fe.conf` .
2.  **Добавьте или измените параметр**. Найдите строку `default_replication_num` и установите значение `1`. Если такой строки нет, добавьте её в конец файла:
    ```properties
    default_replication_num = 1
    ```
3.  **Перезапустите FE-узел**, чтобы изменения вступили в силу :
    ```bash
    # Перейдите в директорию FE
    cd /opt/starrocks/fe
    # Остановите FE
    ./bin/stop_fe.sh
    # Запустите FE
    ./bin/start_fe.sh --daemon
    ```

После этого все новые таблицы будут создаваться с одной репликой.

#### 🛠️ Решение 2: Указать количество реплик при создании таблицы

Если вы не хотите менять глобальные настройки, можно указать нужное число реплик прямо в SQL-запросе на создание таблицы, используя свойства таблицы (`PROPERTIES`).

Вот пример, как должна выглядеть ваша команда `CREATE TABLE`:

```sql
CREATE TABLE IF NOT EXISTS crashdata (
    CRASH_DATE DATETIME,
    BOROUGH STRING,
    -- ... остальные столбцы ...
    VEHICLE_TYPE_CODE_2 STRING
)
PROPERTIES (
    "replication_num" = "1"
);
```

Ключевая часть — это блок `PROPERTIES ("replication_num" = "1")` в конце запроса . Он указывает StarRocks создать только одну копию данных для этой таблицы.

#### 💎 Рекомендация

Проще и надежнее использовать **Решение 1** (изменение `fe.conf`). Это избавит от необходимости каждый раз вручную указывать `replication_num` для каждой новой таблицы.




























