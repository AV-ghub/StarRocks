### 🔍 Основные способы исследования контейнера

Для начала работы вам понадобятся базовые команды Docker.

| Метод | Команда | Что позволяет узнать |
| :--- | :--- | :--- |
| **Интерактивный вход** | `docker exec -it <имя_контейнера> /bin/bash` | Исследовать файловую систему, проверить запущенные процессы, просмотреть логи. |
| **Просмотр логов** | `docker logs <имя_контейнера>` | Увидеть вывод, который контейнер отправляет в стандартные потоки (stdout/stderr), включая ошибки запуска. |

Чтобы узнать имя запущенного контейнера, используется команда `docker ps`. Она покажет список всех активных контейнеров и их имена в колонке **NAMES** .

### 💻 Основные команды для работы с контейнерами

Вот несколько полезных команд для управления и идентификации контейнеров:

| Команда | Описание |
| :--- | :--- |
| `docker ps` | Показать список **запущенных** контейнеров и их имена . |
| `docker ps -a` | Показать список **всех** контейнеров (включая остановленные) и их имена. |
| `docker compose ps` | Показать статус контейнеров, определенных в файле `docker-compose.yml` в текущей директории . |

Имена контейнеров могут быть заданы вручную при запуске с помощью флага `--name`  или автоматически сгенерированы Docker . В составе Docker Compose-проектов имена обычно задаются в конфигурации .

### 🚀 Практическое применение

Например, для подключения к контейнеру с именем `starrocks-fe` используется команда:
```bash
docker exec -it starrocks-fe /bin/bash
```
Если вы хотите дать контейнеру осмысленное имя при запуске, сделайте это с помощью флага `--name` :
```bash
docker run --name my_nginx -d nginx
```

Надеюсь, эти команды помогут вам легко управлять вашими контейнерами. Если вам нужно найти контейнер в рамках конкретного проекта, возможно, потребуется изучить соответствующий `docker-compose.yml` файл.

### 📁 Ключевые каталоги и порты StarRocks

После входа в контейнер, вы найдете компоненты системы по следующим путям:

- **`/opt/module/starrocks/`** – основной каталог установки. Внутри него расположены:
    - **`fe/`** – каталог компонента Frontend (FE)
    - **`be/`** – каталог компонента Backend (BE)
    - **`apache_hdfs_broker/`** – каталок брокера для работы с HDFS
- **`/opt/meta/`** – каталог для хранения метаданных FE (создается при настройке)
- **`/opt/storage/`** – каталог для хранения данных BE (создается при настройке)

StarRocks внутри контейнера использует несколько портов для доступа:

- **Порт 9030** – для подключения MySQL-клиентов и выполнения SQL-запросов
- **Порт 8030** – HTTP-порт FE для административных задач, включая загрузку данных через Stream Load
- **Порт 8040** – веб-интерфейс BE

### 💡 Практические шаги для проверки

Вот что можно сделать после входа в контейнер, чтобы убедиться в его работоспособности:

1.  **Проверить процессы**: Команда `jps` должна показать работающие Java-процессы `StarRocksFe` (и, возможно, `StarRocksBe`), что подтверждает запуск основных компонентов.
2.  **Изучить логи**: В каталогах `fe/log/` и `be/log/` можно найти файлы `fe.log` (или `fe.warn.log`) и `be.INFO.log`. Изучение этих логов — основной способ диагностики проблем при запуске.
3.  **Проверить подключение**: С помощью команды, аналогичной приведенной в результатах поиска, можно подключиться к StarRocks изнутри контейнера: `mysql -P 9030 -h 127.0.0.1 -uroot`. После подключения выполните `SHOW FRONTENDS\G` и `SHOW BACKENDS\G`, чтобы проверить состояние узлов.
