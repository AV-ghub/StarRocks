# Проблема с запуском контейнера starrocks-fe
При попытке [запуска старрокса](https://docs.starrocks.io/docs/quick_start/shared-data/#deploy-starrocks-and-minio)
```
sudo docker compose up --detach --wait --wait-timeout 120
[+] Running 4/4
 ✔ Container minio           Healthy                                                                                                                                                                       6.3s 
 ✔ Container ssc-minio_mc-1  Started                                                                                                                                                                       5.9s 
 ✘ Container starrocks-fe    Error                                                                                                                                                                        36.8s 
 ✔ Container starrocks-cn    Running                                                                                                                                                                       0.0s 
dependency failed to start: container starrocks-fe is unhealthy
```
Проблема с запуском контейнера `starrocks-fe` связана с тем, что Docker помечает его как `unhealthy`.  
Это означает, что сам контейнер запущен, но встроенная проверка здоровья (health check) не проходит.

### 🔍 Первые шаги для диагностики

Чтобы понять конкретную причину, нужно изучить логи и статус проверки здоровья.

1.  **Проверьте детальный статус здоровья контейнера**
    Выполните следующую команду, чтобы увидеть детали проверки: что именно за команда выполняется, какие у нее настройки и почему она завершается с ошибкой.
    ```bash
    sudo docker inspect --format "{{json .State.Health }}" ssc-starrocks-fe-1 | jq
    ```
    Если утилиты `jq` нет, используйте команду без нее:
    ```bash
    sudo docker inspect --format "{{json .State.Health }}" ssc-starrocks-fe-1
    ```
    В выводе ищите параметры `"Status"` (должно быть `"unhealthy"`), `"Log"` (история последних проверок) и `"ExitCode"` (код ошибки).

2.  **Изучите логи контейнера `starrocks-fe`**
    Логи часто содержат самую важную информацию об ошибках инициализации.
    ```bash
    sudo docker compose logs starrocks-fe
    ```
    Просмотрите вывод, особенно обращая внимание на сообщения об ошибках (ERROR) в конце.

### 🛠️ Возможные причины и решения

На основе вашего сценария развертывания, вот наиболее вероятные причины проблемы:

| Причина | Решение |
| :--- | :--- |
| **FE не успевает инициализироваться** (самая частая): Процесс запуска FE (особенно в первый раз) может занимать больше времени, чем отведено в `healthcheck`. | Увеличьте параметры `start_period` и `interval` в настройках `healthcheck` для сервиса `starrocks-fe` в `docker-compose.yml`. |
| **Проблемы с сетью или доступностью портов**: Health check по умолчанию пытается подключиться к порту FE, но что-то ему мешает. | Вручную проверьте команду health check внутри контейнера: `sudo docker compose exec starrocks-fe curl -f http://localhost:8030` (порт может быть другим, см. ваш `docker-compose.yml`). |
| **Конфликт портов или недостаток ресурсов**: Порт, который пытается занять FE, уже занят на хостовой машине, или контейнеру не хватает памяти. | Убедитесь, что порты 8030, 9030 и др. на хосте свободны. Увеличьте лимит оперативной памяти для Docker до **8 ГБ** или более. |

### 💡 Рекомендуемый план действий

1.  **Временно отключите healthcheck**: Модифицируйте ваш `docker-compose.yml` для сервиса `starrocks-fe`, добавив строку `disable: true` в секцию `healthcheck`. Это позволит контейнеру запуститься, и вы сможете спокойно изучить логи.
    ```yaml
    healthcheck:
      disable: true
    ```
2.  **Перезапустите сервис**:
    ```bash
    sudo docker compose up -d starrocks-fe
    ```
3.  **Тщательно изучите логи** с помощью команды `sudo docker compose logs starrocks-fe`, чтобы найти коренную причину.
4.  **Найдите и исправьте проблему** на основе найденных в логах ошибок.
5.  **Верните и настройте healthcheck**: После того как FE станет стабильно запускаться, верните секцию `healthcheck` в `docker-compose.yml`, увеличив таймауты, например:
    ```yaml
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8030"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s  # Увеличенный период на первоначальный старт
    ```
