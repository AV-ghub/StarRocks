# 1
подскажи, если при установке отсюда https://docs.starrocks.io/docs/quick_start/shared-data/
получил в итоге ошибку
Error response from daemon: failed to set up container networking: driver failed programming external connectivity on endpoint minio (5c65c33ad8dff4293f10a18ae3c1d193d6cd19c4022b352a2d80fccbc3e39add): failed to bind host port for 0.0.0.0:9000:172.19.0.2:9000/tcp: address already in use

что логично, ибо у меня установлен кликхаус
как это можно исправить? поправить yaml и перезапустить установку?
как вообще проверить свободные и занятые порты?

Ошибка `address already in use` возникает, когда порт 9000 на вашей машине уже занят другим процессом (в вашем случае — ClickHouse). Решается это изменением конфигурации портов в файле `docker-compose.yml`.

### 🔍 Проверка занятых портов

Чтобы убедиться, что порт 9000 занят, и найти свободные порты, можно использовать следующие команды.

- **Проверить, какой процесс использует порт 9000:**
    ```bash
    sudo lsof -i :9000
    ```
    Или, если `lsof` не установлен:
    ```bash
    sudo netstat -tulpn | grep :9000
    ```
    Команда покажет PID и имя процесса, который занял порт.

- **Проверить статус ClickHouse:**
    ```bash
    sudo systemctl status clickhouse-server
    ```
    Если ClickHouse активен и использует порт 9000, это подтвердит причину конфликта.

### 🛠️ Исправление конфигурации Docker Compose

Чтобы устранить конфликт, нужно изменить маппинг портов для MinIO в файле `docker-compose.yml`, который вы скачали для установки StarRocks.

1.  **Откройте файл `docker-compose.yml`** в текстовом редакторе.
2.  **Найдите секцию, описывающую сервис `minio`.** Она будет выглядеть примерно так:
    ```yaml
    minio:
      # ... другие параметры ...
      ports:
        - 9001:9001
        - 9000:9000
    ```
3.  **Измените внешний порт для `9000`** на любой свободный (например, `9900`):
    ```yaml
      ports:
        - 9001:9001
        - 9900:9000  # Изменяем левую часть (хост:контейнер)
    ```
    Это означает, что порт 9000 внутри контейнера MinIO будет доступен на вашей машине через порт 9900.
4.  **Сохраните файл.**

### 🔄 Перезапуск установки

После внесения изменений необходимо полностью пересоздать окружение, так как простое изменение файла не обновит уже созданные контейнеры.

1.  **Остановите и удалите текущие контейнеры.** Выполните команду в директории с `docker-compose.yml`:
    ```bash
    docker compose down
    ```
2.  **Запустите контейнеры заново:**
    ```bash
    docker compose up -d
    ```
    Теперь MinIO должен запуститься без ошибок, а его веб-интерфейс будет доступен по адресу `http://localhost:9901`, а S3 API — на порту `9900`.

### 💡 Дополнительные замечания

- **Проверьте другие порты:** Убедитесь, что другие порты, которые использует StarRocks (9030, 8030, 8040), также свободны. При необходимости их можно аналогичным образом переназначить в секциях сервисов `starrocks-fe` и `starrocks-be` в том же файле `docker-compose.yml`.
- **Обновите конфигурацию:** После изменения порта MinIO не забудьте использовать новый порт (`9900`) во всех последующих шагах руководства, например, при создании `STORAGE VOLUME` в StarRocks.

# 2

поправил все порты на 95* и 85* соотв по тексту yml
теперь такие ошибки
docker compose logs minio_mc
permission denied while trying to connect to the Docker daemon socket at unix:///var/run/docker.sock: Get "http://%2Fvar%2Frun%2Fdocker.sock/v1.49/containers/json?all=1&filters=%7B%22label%22%3A%7B%22com.docker.compose.config-hash%22%3Atrue%2C%22com.docker.compose.oneoff%3DFalse%22%3Atrue%2C%22com.docker.compose.project%3Dssc%22%3Atrue%2C%22com.docker.compose.service%3Dminio_mc%22%3Atrue%7D%7D": dial unix /var/run/docker.sock: connect: permission denied
[admin@dbcs01 ssc]$ sudo docker compose logs minio_mc
minio_mc-1  | mc: <ERROR> Unable to initialize new alias from the provided credentials. Get "http://minio:9500/probe-bsign-ziyn9u5kvh3ne1mw0ge5tt51rb4ibv/?location=": dial tcp 172.19.0.2:9500: connect: connection refused.
minio_mc-1  | mc: <ERROR> Unable to initialize admin connection. No valid configuration found for 'myminio' host alias.


Ошибка `permission denied while trying to connect to the Docker daemon socket` указывает на проблему с правами доступа к Docker, а `connection refused` говорит о том, что один контейнер не может подключиться к другому. Давайте разберемся с ними по порядку.

### 🔧 Устранение ошибки прав доступа к Docker

Ошибка возникает потому, что процесс внутри контейнера `minio_mc` не имеет достаточных прав для взаимодействия с демоном Docker на хостовой машине через сокет `/var/run/docker.sock`.

**Решение:**

1.  **Проверьте права на сокет Docker.** Убедитесь, что сокет принадлежит группе `docker`, и у этой группы есть необходимые права.
    ```bash
    sudo chown root:docker /var/run/docker.sock
    sudo chmod 660 /var/run/docker.sock
    ```
    

2.  **Убедитесь, что ваш пользователь в группе `docker`.** Команда `groups` должна показывать, что вы в группе `docker`. Если нет, добавьте себя и перелогиньтесь:
    ```bash
    sudo usermod -aG docker $USER
    newgrp docker
    ```
    

3.  **Проверьте, не монтируется ли сокет в `minio_mc`.** Загляните в `docker-compose.yml` — сервис `minio_mc` не должен монтировать `/var/run/docker.sock`, так как ему не требуется управлять Docker.

### 🔌 Устранение ошибки соединения между контейнерами

Ошибка `dial tcp 172.19.0.2:9500: connect: connection refused` означает, что контейнер `minio_mc` не может достучаться до основного контейнера MinIO по адресу `minio:9500`.

**Решение:**

1.  **Убедитесь, что оба контейнера в одной Docker-сети.** Контейнеры, запущенные через `docker compose`, по умолчанию находятся в одной сети и могут разрешать имена друг друга. Проверьте, что в вашем `docker-compose.yml` нет нестандартных настроек сетей для этих сервисов.

2.  **Дождитесь полного запуска MinIO.** Контейнер `minio_mc` может запускаться быстрее, чем основной MinIO успеет поднять сервис на порту 9500. Добавьте в сервис `minio_mc` зависимость от сервиса `minio` и настройте `healthcheck` для MinIO, чтобы `minio_mc` ждал его готовности.
    Пример фрагмента `docker-compose.yml`:
    ```yaml
    services:
      minio:
        image: minio/minio
        container_name: minio
        ports:
          - "9500:9000"  # Пример: внешний порт 9500 мапится на внутренний 9000
        environment:
          MINIO_ACCESS_KEY: USERAKIAIOSFODNN7EXAMPLE
          MINIO_SECRET_KEY: PASSwJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
        command: server /data
        healthcheck: # Может помочь в определении готовности сервиса
          test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
          interval: 30s
          timeout: 20s
          retries: 3

      minio_mc:
        image: minio/mc
        container_name: minio_mc
        depends_on:
          minio:
            condition: service_healthy
        # ... остальные настройки minio_mc
    ```
    

### 💎 Заключение

Решение проблемы состоит из двух основных шагов: настройки прав доступа к Docker на хост-машине и обеспечения корректного взаимодействия между контейнерами в сети Docker через правильные настройки `docker-compose.yml`.

Надеюсь, эти шаги помогут устранить ошибки. Если останутся вопросы — обращайтесь!

# 3 
все сделал, в логах теперь вот так
sudo docker compose logs minio_mc
minio_mc-1  | Added `myminio` successfully.
minio_mc-1  | Access Key: AAAAAAAAAAAAAAAAAAAA
minio_mc-1  | Secret Key: BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB
minio_mc-1  | Expiration: no-expiry

порты вот так
sudo netstat -tulpn | grep :9
...
tcp        0      0 0.0.0.0:9501            0.0.0.0:*               LISTEN      2307394/docker-prox 
tcp        0      0 0.0.0.0:9500            0.0.0.0:*               LISTEN      2307379/docker-prox 
tcp        0      0 0.0.0.0:9530            0.0.0.0:*               LISTEN      2308781/docker-prox 
tcp        0      0 0.0.0.0:9520            0.0.0.0:*               LISTEN      2308766/docker-prox 

но при этом на http://localhost:9501/access-keys зайти не могу, отказ
и
sudo docker compose exec starrocks-fe mysql -P9530 -h127.0.0.1 -uroot --prompt="StarRocks > "
ERROR 2003 (HY000): Can't connect to MySQL server on '127.0.0.1:9530' (111)

Отлично, что `minio_mc` успешно выполнился и создал credentials! Это значит, что MinIO внутри контейнера работает. Теперь разберемся с проблемами доступа.

### 🔍 Анализ проблем

1. **MinIO Web UI недоступен на localhost:9501**
2. **MySQL-клиент не может подключиться к StarRocks на localhost:9530**

Обе проблемы связаны с сетевыми настройками в Docker Compose.

### 🛠️ Решение проблемы с MinIO Web UI

Порт 9501 проброшен, но MinIO внутри контейнера по умолчанию слушает **порт 9001** для консоли. Нужно проверить конфигурацию:

```bash
# Проверим логи MinIO - на каком порту он запустил консоль
sudo docker compose logs minio | grep -i "console"
```

Если в логах видно, что консоль доступна на `http://0.0.0.0:9001`, значит нужно исправить порты в `docker-compose.yml`:

```yaml
minio:
  image: minio/minio
  ports:
    - "9500:9000"   # S3 API
    - "9501:9001"   # Web UI
  # ... остальные настройки
```

После изменений:
```bash
sudo docker compose down
sudo docker compose up -d minio
sudo docker compose logs minio
```

### 🗄️ Решение проблемы подключения к StarRocks

Ошибка `ERROR 2003 (HY000): Can't connect to MySQL server` обычно означает, что FE еще не запустился полностью.

**Шаги диагностики:**

1. **Проверим статус FE-узла:**
```bash
# Смотрим логи FE - ищем сообщение о готовности
sudo docker compose logs starrocks-fe | grep -i "start"
```

2. **Проверим, слушает ли FE порт:**
```bash
# Проверим изнутри контейнера
sudo docker compose exec starrocks-fe netstat -tulpn | grep 9030
```

3. **Подключимся изнутри контейнера:**
```bash
# Попробуем подключиться из контейнера FE к самому себе
sudo docker compose exec starrocks-fe mysql -P9030 -h127.0.0.1 -uroot
```

4. **Если работает изнутри, но не с хоста - пробуем другой подход:**
```bash
# Подключимся через IP контейнера
sudo docker compose exec starrocks-fe hostname -I
# Затем используем этот IP вместо 127.0.0.1
```

### 💡 Альтернативный способ подключения

Если прямое подключение не работает, попробуйте подключиться через контейнер:

```bash
# Заходим в контейнер FE и подключаемся оттуда
sudo docker compose exec -it starrocks-fe /bin/bash

# Внутри контейнера:
mysql -P9030 -h127.0.0.1 -uroot
```

### 📋 Быстрая проверка всех сервисов

Давайте проверим статус всех контейнеров:

```bash
sudo docker compose ps

# Проверим, какие порты действительно слушают контейнеры
sudo docker compose exec starrocks-fe netstat -tulpn | grep LISTEN
sudo docker compose exec minio netstat -tulpn | grep LISTEN
```

**Что скорее всего произошло:**
- MinIO: порты в `docker-compose.yml` могут не соответствовать реальным портам внутри контейнера
- StarRocks FE: сервис еще не полностью инициализировался и не готов принимать подключения

Попробуйте эти диагностические шаги и покажите вывод - это поможет точнее определить проблему!


# 4
sudo docker compose logs starrocks-fe | grep -i "start"
ничего не возвращает
sudo docker compose exec starrocks-fe netstat -tulpn | grep 9030
ничего не возвращает

изнутри подключаемся любым способом успешно

и альтернативным способом все успешно

sudo docker compose ps
NAME           IMAGE                            COMMAND                  SERVICE        CREATED          STATUS                    PORTS
minio          minio/minio:latest               "sh -c 'mkdir -p /mi…"   minio          17 minutes ago   Up 17 minutes (healthy)   0.0.0.0:9500->9000/tcp, [::]:9500->9000/tcp, 0.0.0.0:9501->9001/tcp, [::]:9501->9001/tcp
starrocks-cn   starrocks/cn-ubuntu:3.5-latest   "/bin/bash -c 'sleep…"   starrocks-cn   17 minutes ago   Up 17 minutes (healthy)   0.0.0.0:8540->8040/tcp, [::]:8540->8040/tcp
starrocks-fe   starrocks/fe-ubuntu:3.5-latest   "/bin/bash -c 'echo …"   starrocks-fe   17 minutes ago   Up 17 minutes (healthy)   0.0.0.0:8530->8030/tcp, [::]:8530->8030/tcp, 0.0.0.0:9520->9020/tcp, [::]:9520->9020/tcp, 0.0.0.0:9530->9030/tcp, [::]:9530->9030/tcp


sudo docker compose exec starrocks-fe netstat -tulpn | grep LISTEN
tcp        0      0 127.0.0.11:38209        0.0.0.0:*               LISTEN      -                   
tcp6       0      0 :::6090                 :::*                    LISTEN      1/java              
tcp6       0      0 :::8030                 :::*                    LISTEN      1/java              
tcp6       0      0 :::9020                 :::*                    LISTEN      1/java              
tcp6       0      0 :::9010                 :::*                    LISTEN      1/java              
tcp6       0      0 :::9030                 :::*                    LISTEN      1/java              


sudo docker compose exec minio netstat -tulpn | grep LISTEN
возвращает пустую строку


Отлично! Теперь ситуация прояснилась. Проблема **не в сервисах**, а в **сетевой конфигурации Docker**.

### 🔍 Анализ ситуации

Из вашего вывода видно:

1. **StarRocks FE работает корректно** - слушает порт 9030 внутри контейнера
2. **Проброс портов настроен** - 9530:9030 в `docker-compose ps`
3. **Подключение изнутри контейнера работает**
4. **Но с хоста подключение не работает**

### 🐛 Причина проблемы

StarRocks FE в контейнере слушает на **IPv6 адресе** (`:::9030`), а не на IPv4. Когда вы подключаетесь с хоста через `127.0.0.1:9530` (IPv4), соединение не проходит.

### 🛠️ Решения

**Решение 1: Подключение через IPv6 с хоста**
```bash
mysql -P9530 -h::1 -uroot
```

**Решение 2: Заставить FE слушать на IPv4**
Добавьте в конфигурацию FE (в `docker-compose.yml`):
```yaml
starrocks-fe:
  image: starrocks/fe-ubuntu:3.5-latest
  command: >
    /bin/bash -c "
    echo 'priority_networks=172.*.*.*/16' >> /opt/starrocks/fe/conf/fe.conf &&
    /opt/starrocks/fe/bin/start_fe.sh --daemon &&
    tail -f /opt/starrocks/fe/log/fe.log"
```

**Решение 3: Подключение через имя сервиса Docker**
```bash
# Из хоста, но используя Docker network
sudo docker compose exec starrocks-fe mysql -P9030 -hstarrocks-fe -uroot
```

**Решение 4: Использовать IP контейнера**
```bash
# Узнаем IP контейнера FE
sudo docker inspect -f '{{range.NetworkSettings.Networks}}{{.IPAddress}}{{end}}' ssc-starrocks-fe-1

# Подключаемся по этому IP (пример)
mysql -P9030 -h172.19.0.3 -uroot
```

### 🌐 Проверка MinIO

Для MinIO похожая ситуация - проверим доступность:
```bash
# Проверим изнутри контейнера
sudo docker compose exec minio curl -s http://localhost:9001/minio/health/live

# Проверим с хоста через IPv6
curl -s http://[::1]:9501/minio/health/live
```

### 💡 Рекомендация

Самый простой способ - **Решение 1** (подключение через IPv6). Попробуйте:
```bash
mysql -P9530 -h::1 -uroot
```

Если это не сработает, используйте **Решение 4** с конкретным IP контейнера.

MinIO консоль тогда будет доступна по: `http://[::1]:9501`

**Дополнительная проверка:** 
```bash
# Проверим, виден ли порт 9530 с хоста
sudo netstat -tulpn | grep 9530
# Должен быть виден как прослушиваемый
```

Попробуйте подключение через IPv6 и сообщите о результате!


# 5
mysql -P9530 -h::1 -uroot
bash: mysql: команда не найдена...
Пакеты, предоставляющие этот файл:
'mariadb'
'mysql'


Понятно! Команда `mysql` не установлена на вашей хостовой машине. Нужно либо установить MySQL-клиент, либо использовать альтернативные способы подключения.

### 💻 Установка MySQL-клиента

**Для CentOS/RHEL/Fedora:**
```bash
sudo yum install mysql
# или
sudo dnf install mysql
```

**Для Ubuntu/Debian:**
```bash
sudo apt-get install mysql-client
```

### 🐳 Альтернативные способы подключения (без установки клиента на хост)

**Способ 1: Подключение из контейнера StarRocks-FE**
```bash
sudo docker compose exec starrocks-fe mysql -P9030 -h127.0.0.1 -uroot
```

**Способ 2: Использование временного MySQL-контейнера**
```bash
sudo docker run -it --rm --network ssc_default mysql:8.0 mysql -P9530 -hstarrocks-fe -uroot
```

**Способ 3: Зайти в контейнер FE и подключиться оттуда**
```bash
# Заходим в контейнер
sudo docker compose exec -it starrocks-fe /bin/bash

# Внутри контейнера подключаемся к FE
mysql -P9030 -h127.0.0.1 -uroot
```

### 🔧 Проверка доступности порта с хоста

Перед подключением убедимся, что порт действительно доступен:
```bash
# Проверяем доступность порта 9530
telnet ::1 9530
# или
nc -zv ::1 9530

# Если эти команды не работают, пробуем IPv4
telnet 127.0.0.1 9530
```

### 💡 Рекомендация

Самый простой вариант - **Способ 1** (подключение из контейнера FE), так как там уже установлен MySQL-клиент и настроено сетевое соединение.

После успешного подключения вы должны увидеть приглашение:
```
StarRocks >
```

И сможете выполнять SQL-запросы для проверки работы кластера, например:
```sql
SHOW FRONTENDS;
SHOW BACKENDS;
```

Какой способ подключения предпочтете?

# 6
mysql -P9530 -h::1 -uroot
Welcome to the MySQL monitor.  Commands end with ; or \g.
Your MySQL connection id is 16777564
Server version: 8.0.33 3.5.7-d2da301

Copyright (c) 2000, 2025, Oracle and/or its affiliates.

Oracle is a registered trademark of Oracle Corporation and/or its
affiliates. Other names may be trademarks of their respective
owners.

Type 'help;' or '\h' for help. Type '\c' to clear the current input statement.

mysql> \q
Bye

Отлично! Подключение к StarRocks FE работает через IPv6. Теперь давайте разберемся с MinIO.

### 🌐 Проверка MinIO Web UI

Попробуйте открыть MinIO консоль через IPv6:

**В браузере перейдите по адресу:**
```
http://[::1]:9501
```

**Или через терминал проверим доступность:**
```bash
curl -s http://[::1]:9501/minio/health/live
```

### 🔑 Доступ к MinIO

Если Web UI доступен, используйте credentials которые вы видели в логах:
- **Access Key**: `AAAAAAAAAAAAAAAAAAAA` 
- **Secret Key**: `BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB`

### 🗄️ Проверка состояния StarRocks

Теперь когда подключение к FE работает, проверим состояние кластера:

```bash
mysql -P9530 -h::1 -uroot -e "SHOW FRONTENDS; SHOW BACKENDS;"
```

Или интерактивно:
```bash
mysql -P9530 -h::1 -uroot
```

```sql
-- Проверим FE узлы
SHOW FRONTENDS\G

-- Проверим BE узлы  
SHOW BACKENDS\G

-- Проверим CN узлы (если используете shared-data)
SHOW COMPUTE NODES\G
```

### 📋 Что должно быть в норме

- **FE**: `Alive: true`
- **BE/CN**: `Alive: true`, `SystemDecommissioned: false`

### 🔧 Если MinIO все еще недоступен

Проверим логи MinIO:
```bash
sudo docker compose logs minio | tail -20
```

И проверим, отвечает ли MinIO API:
```bash
curl -s http://[::1]:9500/minio/health/live
```

### 💡 Итог

- **StarRocks подключение**: ✅ Работает через `mysql -P9530 -h::1 -uroot`
- **MinIO консоль**: Проверяем через `http://[::1]:9501`
- **MinIO API**: Проверяем через `http://[::1]:9500`

Как результаты проверки MinIO? Доступна ли консоль и API?

# 7

http://[::1]:9501/
Соединение было сброшено


Проблема с MinIO. Давайте диагностируем, что происходит с контейнером MinIO.

### 🔍 Диагностика MinIO

**1. Проверим статус контейнера MinIO:**
```bash
sudo docker compose ps minio
```

**2. Проверим логи MinIO - что происходит при запуске:**
```bash
sudo docker compose logs minio --tail=50
```

**3. Проверим, слушает ли MinIO порты внутри контейнера:**
```bash
sudo docker compose exec minio netstat -tulpn
```

**4. Проверим здоровье MinIO:**
```bash
sudo docker compose exec minio curl -s http://localhost:9000/minio/health/live
```

### 🐛 Возможные причины

Из того что мы видим:
- Порт проброшен (`9501:9001`)
- Но соединение сбрасывается

**Частые причины:**
1. **MinIO не запустился** - ошибка в конфигурации
2. **MinIO слушает не на тех портах** - возможно изменились порты в новой версии
3. **Проблема с IPv6** - MinIO может не слушать на IPv6

### 🛠️ Быстрое решение

**Проверим через IPv4:**
```bash
curl -s http://127.0.0.1:9501/minio/health/live
```

**Или попробуйте в браузере:**
```
http://127.0.0.1:9501
```

### 🔧 Альтернативное решение - пересоздание MinIO

Если проблема persists, попробуем пересоздать MinIO:

```bash
# Останавливаем и удаляем только MinIO
sudo docker compose stop minio minio_mc
sudo docker compose rm -f minio minio_mc

# Запускаем заново
sudo docker compose up -d minio

# Ждем 30 секунд и проверяем логи
sleep 30
sudo docker compose logs minio --tail=20
```

### 💡 Проверка через S3 API

Попробуем проверить MinIO через S3 API (порт 9500):
```bash
# Используем mc из контейнера для проверки
sudo docker compose run --rm minio_mc mc alias list myminio
```

### 📋 Что ищем в логах

В логах MinIO ищем:
- `Console endpoint` - на каком порту доступна консоль
- `Object API` - на каком порту S3 API
- Ошибки инициализации

**Покажите вывод этих команд, особенно логи MinIO - это поможет точно определить проблему!**

Какие результаты диагностики?

# 8
итого
sudo docker compose logs minio --tail=50
minio  | INFO: Formatting 1st pool, 1 set(s), 1 drives per set.
minio  | INFO: WARNING: Host local has more than 0 drives of set. A host failure will result in data becoming unavailable.
minio  | MinIO Object Storage Server
minio  | Copyright: 2015-2025 MinIO, Inc.
minio  | License: GNU AGPLv3 - https://www.gnu.org/licenses/agpl-3.0.html
minio  | Version: RELEASE.2025-09-07T16-13-09Z (go1.24.6 linux/amd64)
minio  | 
minio  | API: http://172.19.0.2:9000  http://127.0.0.1:9000 
minio  | WebUI: http://172.19.0.2:9501 http://127.0.0.1:9501  
minio  | 
minio  | Docs: https://docs.min.io

и http://172.19.0.2:9501 прекрасно работает
а http://127.0.0.1:9501  - нет

Отлично! Проблема определена. MinIO запустился и работает, но слушает **только на внутренних адресах Docker сети**:

- **API**: `http://172.19.0.2:9000` и `http://127.0.0.1:9000`
- **WebUI**: `http://172.19.0.2:9501` и `http://127.0.0.1:9501`

Но порт `127.0.0.1:9501` внутри контейнера **не проброшен на хост**.

### 🛠️ Решение

Нужно изменить конфигурацию MinIO в `docker-compose.yml`, чтобы он слушал на `0.0.0.0`:

```yaml
minio:
  image: minio/minio
  command: server /data --console-address ":9501"  # Явно указываем порт консоли
  environment:
    MINIO_ACCESS_KEY: USERAKIAIOSFODNN7EXAMPLE
    MINIO_SECRET_KEY: PASSwJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
  ports:
    - "9500:9000"
    - "9501:9501"  # Меняем 9001 на 9501
  healthcheck:
    test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
    interval: 30s
    timeout: 20s
    retries: 3
```

**После изменений:**
```bash
sudo docker compose down minio minio_mc
sudo docker compose up -d minio
```

### 🔧 Альтернативное решение (быстрое)

Используйте внутренний IP для доступа к MinUI:
```
http://172.19.0.2:9501
```

Или пробросьте порт временно:
```bash
sudo docker compose exec minio apk add --no-cache curl 2>/dev/null || sudo docker compose exec minio apt-get update && apt-get install -y curl
sudo docker compose port minio 9501
```

### 🌐 Для StarRocks - аналогичная проблема

Возможно, у StarRocks FE похожая ситуация. Проверим:
```bash
sudo docker compose logs starrocks-fe | grep -E "(API|WebUI|http|port)"
```

### 💡 Временное решение

Пока настраиваете `docker-compose.yml`, можете использовать прямое подключение к контейнеру MinIO через его внутренний IP:
```
http://172.19.0.2:9501
```

**Логин:** `AAAAAAAAAAAAAAAAAAAA`  
**Пароль:** `BBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBBB`

Попробуйте доступ через `172.19.0.2:9501` - это должно работать сразу!

# 9
ну там как раз вот так
services:
  minio:
    container_name: minio
    environment:
      MINIO_ROOT_USER: miniouser
      MINIO_ROOT_PASSWORD: miniopassword
    image: minio/minio:latest
    ports:
      - "9501:9001"
      - "9500:9000"
    entrypoint: sh
    command: '-c ''mkdir -p /minio_data/starrocks && minio server /minio_data --console-address ":9501"'''

А, теперь понятно! Проблема в том, что в `command` вы явно указали `--console-address ":9501"` для MinIO, но в секции `ports` маппинг остался `"9501:9001"`.

MinIO теперь слушает консоль на порту **9501 внутри контейнера**, а не на 9001.

### 🛠️ Исправление

Нужно изменить маппинг портов в `docker-compose.yml`:

```yaml
services:
  minio:
    container_name: minio
    environment:
      MINIO_ROOT_USER: miniouser
      MINIO_ROOT_PASSWORD: miniopassword
    image: minio/minio:latest
    ports:
      - "9501:9501"  # Было: "9501:9001", стало: "9501:9501"
      - "9500:9000"
    entrypoint: sh
    command: '-c ''mkdir -p /minio_data/starrocks && minio server /minio_data --console-address ":9501"'''
```

### 🔄 Перезапуск

После исправления:
```bash
sudo docker compose down minio
sudo docker compose up -d minio
```

### ✅ Проверка

После перезапуска проверьте:
```bash
# Логи MinIO
sudo docker compose logs minio --tail=5

# Доступность Web UI
curl -s http://127.0.0.1:9501/minio/health/live

# Или в браузере
http://localhost:9501
```

### 🔍 Почему это произошло

- **Раньше**: MinIO по умолчанию использовал порт 9001 для консоли
- **Сейчас**: Вы явно указали `--console-address ":9501"`, поэтому консоль слушает на 9501 внутри контейнера
- **Маппинг**: Должен соответствовать - внешний порт 9501 → внутренний порт 9501

После этого исправления Web UI MinIO должен стать доступен по `http://localhost:9501`!    





