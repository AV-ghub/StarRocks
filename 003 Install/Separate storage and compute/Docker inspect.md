Чтобы узнать путь, по которому развернулся контейнер на хостовой машине, нужно смотреть в двух направлениях: путь к данным самого контейнера и пути к подключенным томам (volumes).

### 🔍 Основные команды для проверки путей

Для получения информации используется команда `docker inspect`. Вот как применить её на практике.

| Что нужно узнать | Команда | Что показывает |
| :--- | :--- | :--- |
| **Пути к данным контейнера (слои)** | `docker inspect -f '{{ .GraphDriver.Data.MergedDir }}' <имя_контейнера>` | **Рабочая файловая система** контейнера. Все файлы, которые видит контейнер, объединены здесь. |
| **Пути к томам (Volumes)** | `docker inspect -f '{{ range .Mounts }}{{ .Source }} -> {{ .Destination }}{{ "\n" }}{{ end }}' <имя_контейнера>` | **Папки на хосте** (Source) и соответствующие им **точки монтирования внутри контейнера** (Destination). |
| **Корневая директория Docker** | `docker info | grep "Docker Root Dir"` | Базовый путь, где Docker хранит все данные (контейнеры, образы, тома). По умолчанию `/var/lib/docker`. |

### 💡 Практическое применение команд

Допустим, у вас запущен контейнер с именем `myapp`.

1.  **Чтобы увидеть его рабочую файловую систему**, выполните:
    ```bash
    docker inspect -f '{{ .GraphDriver.Data.MergedDir }}' myapp
    ```
    Команда вернет путь вроде `/var/lib/docker/overlay2/a1b2c3.../merged`. Это и есть "корень" контейнера на вашей машине.

2.  **Чтобы найти все смонтированные тома**, выполните:
    ```bash
    docker inspect -f '{{ range .Mounts }}{{ .Source }} -> {{ .Destination }}{{ "\n" }}{{ end }}' myapp
    ```
    Вы увидите, какие папки с вашего компьютера подключены и в какие папки внутри контейнера.

3.  **Глобальное расположение данных Docker** можно проверить так:
    ```bash
    docker info | grep "Docker Root Dir"
    ```
    Это полезно, если вы хотите понять, в каком разделе диска находятся все данные Docker.

### ⚠️ Важное замечание

На операционных системах **macOS и Windows** Docker работает внутри легковесной виртуальной машины (VM). Пути, которые вы видите через `docker inspect` (например, `/var/lib/docker/...`), являются путями **внутри этой VM**, а не в вашей основной файловой системе.

- На **macOS** данные VM лежат в `~/Library/Containers/com.docker.docker/Data/vms/0/`.
- На **Windows** — в `C:\ProgramData\DockerDesktop`.

