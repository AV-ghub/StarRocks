StarRocks поддерживает установку без контейнеров.  
Такой способ развертывания предоставляет больше контроля над конфигурацией и лучше подходит для производственных сред, но при этом требует больше ручных шагов по настройке.

[Deploy StarRocks manually](https://docs.starrocks.io/docs/deployment/deploy_manually/)   
[Как развернуть StarRocks — одноузловое развёртывание](https://tproger.ru/articles/glava-1-3--razvertyvanie-starrocks---odnouzlovoe-razvertyvanie)   
[Развертывание StarRocks — сборка из исходников](https://tproger.ru/articles/glava-1-1--razvertyvanie-starrocks---sborka-iz-ishodnikov)   



## ⚙️ Ручное развертывание: основные шаги

Процесс установки состоит из последовательного запуска и настройки двух типов узлов: Frontend (FE) и Backend (BE). Для тестовой среды можно развернуть оба компонента на одной машине.

**Таблица: Этапы ручного развертывания StarRocks**

| Этап | Ключевые действия | Важные замечания |
| :--- | :--- | :--- |
| **1. Запуск FE-узла** | - Создать отдельный каталог для метаданных (например, `/opt/meta`).<br>- Настроить файл `fe/conf/fe.conf`: указать `meta_dir` и, при необходимости, `priority_networks` для IP-адреса.<br>- Выполнить скрипт запуска: `./fe/bin/start_fe.sh --daemon`. | FE — точка входа для SQL-запросов и хранилище метаданных. |
| **2. Запуск BE-узла** | - Создать отдельный каталог для данных (например, `/opt/storage`).<br>- Настроить файл `be/conf/be.conf`: указать `storage_root_path` и, при необходимости, `priority_networks`.<br>- Выполнить скрипт запуска: `./be/bin/start_be.sh --daemon`. | BE отвечает за хранение данных и выполнение запросов. |
| **3. Настройка кластера** | - Подключиться к FE через MySQL-клиент: `mysql -h ... -P 9030 -uroot`.<br>- Добавить BE-узел в кластер SQL-командой: `ALTER SYSTEM ADD BACKEND 'ip_адрес:9050';`. | Порт по умолчанию для сердцебиения BE — 9050. |

После выполнения этих шагов можно проверить состояние узлов. Для FE команда `SHOW PROC '/frontends'\G` должна показать `Alive: true`. Для BE можно проверить логи: `cat be/log/be.INFO | grep heartbeat`.

### 💽 Подготовка к установке

Перед началом установки важно учесть несколько моментов:

- **Получение дистрибутива**: Можно скачать готовые бинарные пакеты с официального сайта для архитектуры x86 или собрать из исходников, что актуально для ARM.
- **Требования к CPU**: Для оптимальной работы BE-узлов процессор должен поддерживать набор инструкций **AVX2**. Проверить это можно командой `cat /proc/cpuinfo | grep avx2`.
- **Выбор архитектуры**: StarRocks поддерживает две архитектуры:
    - **Shared-nothing (рекомендуется для начала)**: Каждый BE-узел хранит данные на своем локальном диске. Это классическая высокопроизводительная MPP-архитектура.
    - **Shared-data**: Данные хранятся в удаленном объектном хранилище (например, S3), а узлы (CN) являются безсостоятельными. Эта архитектура более эластична, но может быть сложнее в начальной настройке.

### 💎 Сравнение способов установки

- **Контейнерная установка**: Максимально проста и идеальна для быстрого знакомства или тестирования. Она скрывает многие детали конфигурации.
- **Бесконтейнерная установка**: Требует больше ручных шагов, но дает полный контроль над конфигурацией, производительностью и расположением данных, что критически важно для производственных рабочих нагрузок.

## Необходимые аппаратные ресурсы
**Да, вы абсолютно можете поднять FE-узел и несколько BE-узлов на одной виртуальной машине для тестирования!** Это стандартная и широко рекомендуемая практика.

### 🟢 Почему это возможно и правильно

Архитектура StarRocks не требует, чтобы узлы обязательно находились на разных физических машинах. Все узлы общаются друг с другом по сети, и им безразлично, находится ли эта сеть между разными серверами или внутри одного (loopback). Для тестового стенда это идеальный подход.

### ⚙️ Как это правильно настроить

Главное — правильно настроить конфигурацию, чтобы узлы не конфликтовали друг с другом за ресурсы.

| Аспект настройки | Ключевые моменты и рекомендации |
| :--- | :--- |
| **Порты** | Каждому экземпляру (FE или BE) нужен свой уникальный набор портов. Вы не можете запустить два BE на одних и тех же портах. |
| **Директории данных** | Каждому узлу (FE и каждому BE) должна быть назначена своя, отдельная директория для метаданных и данных. Не используйте один и тот же путь. |
| **Конфигурация** | В конфигурационных файлах (`fe.conf`, `be.conf`) для каждого экземпляра необходимо явно указать его уникальные порты и пути к данным. |

### 🚀 Практический пример запуска

Допустим, вы хотите поднять 1 FE и 2 BE на одной машине. Вот упрощенная последовательность действий:

1.  **Подготовка директорий:**
    ```bash
    mkdir -p /opt/starrocks/fe/{meta,log}
    mkdir -p /opt/starrocks/be1/{storage,log}
    mkdir -p /opt/starrocks/be2/{storage,log}
    ```

2.  **Запуск FE (порты по умолчанию):**
    *   В `fe/conf/fe.conf` укажите `meta_dir = /opt/starrocks/fe/meta`.
    *   Запустите: `./fe/bin/start_fe.sh --daemon`.

3.  **Запуск первого BE (BE1):**
    *   В `be1/conf/be.conf` укажите:
        *   `storage_root_path = /opt/starrocks/be1/storage`
        *   `be_port = 9050` (порт по умолчанию)
        *   `webserver_port = 8040` (порт по умолчанию)
    *   Запустите: `./be1/bin/start_be.sh --daemon`.

4.  **Запуск второго BE (BE2):**
    *   В `be2/conf/be.conf` укажите:
        *   `storage_root_path = /opt/starrocks/be2/storage`
        *   `be_port = 9060` (**измененный** порт)
        *   `webserver_port = 8041` (**измененный** порт)
    *   Запустите: `./be2/bin/start_be.sh --daemon`.

5.  **Добавление BE в кластер:**
    *   Подключитесь к FE через MySQL-клиент.
    *   Выполните команды:
        ```sql
        ALTER SYSTEM ADD BACKEND "be1_host_ip:9050";
        ALTER SYSTEM ADD BACKEND "be2_host_ip:9060";
        ```
    *   В качестве `be_host_ip` можно использовать `127.0.0.1` или внутренний IP виртуальной машины.

### ⚠️ Важные замечания

*   **Производительность:** Не ожидайте высокой производительности, так как все узлы делят ресурсы одной машины (CPU, RAM, дисковую подсистему). Это компромисс для удобства тестирования.
*   **Отказоустойчивость:** Такой кластер не будет отказоустойчивым, так как выход из строя одной виртуальной машины приведет к падению всех узлов. Но для тестов это приемлемо.
*   **Docker Compose:** Еще проще для тестирования использовать готовый `docker-compose.yml` файл, где вся работа по настройке портов и томов уже сделана за вас. Официальная документация StarRocks часто предоставляет такие примеры.

**Вывод:** Смело поднимайте все узлы на одной машине. Это отличный способ быстро познакомиться с функционалом StarRocks, проверить работу кластера и отточить навыки администрирования перед развертыванием в продакшене на разных серверах.
