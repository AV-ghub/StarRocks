# Количество JOIN в одном запросе
```
в мире OLTP есть неофициальное правило не превышать количество джойнов больше 7-8 из-за потенциальной деградации
есть ли такие ограничения в старроксе?
т.е. что лучше сделать архитектурно - пару-тройку широких денормализованных витрин и использовать их потом в объединениях, или 10-20-30 таблиц для той же цели?
что из этих вариантов лучше переварит старрокс, что более будет являться рекомендуемой архитектурой?
```
В StarRocks **нет жесткого ограничения** на количество JOIN в одном запросе, подобного "правилу 7-8" из мира OLTP. StarRocks специально создан для эффективного выполнения сложных соединений множества таблиц, что является сильной стороной его архитектуры.

### ⚙️ Почему StarRocks справляется со множественными JOIN

Архитектурные особенности StarRocks позволяют ему эффективно обрабатывать сложные запросы:

1.  **Мощный Cost-Based Optimizer (CBO)**: StarRocks использует cost-based оптимизатор, который анализирует статистику данных (размер таблиц, селективность предикатов) для поиска оптимального плана выполнения запроса с множеством JOIN, включая автоматический **reorder JOIN** для минимизации промежуточных результатов.

2.  **Различные стратегии выполнения JOIN**: Система автоматически выбирает наиболее эффективную стратегию:
    *   **Colocate Join**: Данные соединяемых таблиц физически расположены на одних и тех же узлах, что позволяет выполнять JOIN локально без сетевой пересылки данных.
    *   **Broadcast Join**: Малая таблица "транслируется" на все узлы, где хранятся части большой таблицы.
    *   **Shuffle Join (Hash Partitioning)**: Данные между узлами перераспределяются по ключу JOIN для параллельного выполнения.
    *   **Bucket Shuffle Join**: Данные из правой таблицы пересылаются только на те узлы, где хранятся соответствующие бакеты левой таблицы.

3.  **Векторный движок выполнения (Vectorized Execution Engine)**: Обработка данных блоками (векторами) вместо построчной значительно повышает эффективность использования CPU.

### 🏗️ Рекомендации по проектированию

Для сложной аналитики предпочтительнее **нормализованная схема (звезда/снежинка)**.

| Подход | Нормализованная схема (Звезда/Снежинка) | Денормализованная схема (Широкая таблица) |
| :--- | :--- | :--- |
| **Описание** | Множество таблиц, соединенных по ключам. | Несколько очень широких таблиц, объединяющих данные из разных сущностей. |
| **Гибкость** | **Высокая**. Легко добавлять новые измерения, изменять атрибуты. | **Низкая**. Любое изменение требует перестройки всей таблицы и пайплайнов. |
| **Актуальность данных** | **Высокая**. Обновление происходит только в одной таблице-измерении. | **Низкая**. Обновление измерения требует перезаписи всех связанных фактов. |
| **Сложность ETL** | **Проще**. Пайплайны загрузки данных логически разделены. | **Сложнее**. Требуется сложная логика сборки одной широкой записи. |
| **Производительность запросов** | **Высокая** при правильном проектировании и использовании сильных сторон StarRocks (JOIN). | **Высокая** для простых запросов, но может деградировать при необходимости сканирования широкой таблицы. |
| **Потребление места** | **Эффективное**. Данные измерений не дублируются. | **Избыточное**. Атрибуты измерений дублируются в каждой строке фактов. |

### 💡 Выводы и рекомендации

1.  **Выбирайте нормализованную схему**. Для аналитических workloads в StarRocks это более гибкий, эффективный и рекомендуемый подход. Архитектура StarRocks оптимизирована именно для такой работы.

2.  **Правильно настраивайте таблицы**. Используйте **Colocate Join** и **продуманное бакетирование** (DISTRIBUTED BY HASH) по ключам соединения, чтобы минимизировать движение данных между узлами.

3.  **Доверяйте оптимизатору**. CBO StarRocks эффективно справляется с десятками JOIN. Ваша задача — предоставить ему актуальную статистику через команду `ANALYZE TABLE`.

