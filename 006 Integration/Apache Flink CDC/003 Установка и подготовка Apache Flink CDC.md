# Установка и подготовка Apache Flink CDC
> Итак, подытожив выкладки, для того чтобы запустить Flink CDC YAML, нам небходимо предварительно:
> * установить и запустить кластер флинк со всеми нужными коннекторами в /lib
> * после этого мы не используем [/smt](https://docs.starrocks.io/docs/integrations/loading_tools/SMT/#synchronize-postgresql-to-starrocks) с его генерациями скриптов, а ставим каким-то образом декларативную надстройку Flink CDC YAML
> * и конфигурируем ее
> * после этого стартуем скрипт с настроенным yaml файлом в параметре
> * он выстраивает весь пайплайн:
>   - идет в указанный источник ->
>   - создает там репликационный слот (при этом wal_level = logical уже должен быть там включен) ->
>   - делает и запускает репликацию (возможно по прописке в yaml?) ->
>   - создает подписку -> настраивает дебезиум (возможно тоже по некоторой прописке в yaml?) ->
>   - настраивает коннектор и потребитель (старрокс) ->
>   - создает джобу и задание ->
>   - запускает их ->
>   - каким-то образом отслеживает процесс и состояния ->
>   - реагирует на изменения, ведет логи в процессе работы
Перейдем ко всем этим деталям и погрузимся в параметры настройки.

Погрузимся в детали настройки Flink CDC YAML на примере PostgreSQL → StarRocks, шаг за шагом.

### 🛠️ Шаг 1: Установка и подготовка Flink
Этот шаг вы описали верно.

1.  **Установите Apache Flink** (рекомендуется версия 1.17+).
2.  **Скачайте Flink CDC 3.5+** с [официального сайта](https://downloads.apache.org/flink/flink-cdc-3.5.0/) и распакуйте.
3.  **Скопируйте JAR-файлы** из `flink-cdc-3.5.0/lib/` в директорию `lib/` вашего кластера Flink. **Важно**: Для работы с PostgreSQL и StarRocks вам понадобятся:
    *   `flink-sql-connector-postgres-cdc-*.jar` (источник)
    *   `flink-connector-starrocks-*.jar` (приемник)
    *   `flink-cdc-pipeline-*.jar` (ядро декларативной надстройки)

### 📄 Шаг 2: Создание YAML-конфигурации
Вот пример конфигурационного файла `postgres-to-starrocks.yaml`. Комментарии объясняют ключевые секции:

```yaml
# postgres-to-starrocks.yaml

source:
  # Тип источника - PostgreSQL
  type: postgres
  # Параметры подключения к PostgreSQL (обязательные)
  hostname: localhost
  port: 5432
  username: flink_user
  password: your_secure_password
  database-name: my_database
  schema-name: public
  # Ключевые параметры управления репликацией
  slot.name: flink_cdc_slot # Имя слота репликации. Можно задать своё.
  # Выбор таблиц: синхронизируем почти все (.*), но исключаем служебные
  table-list: ["public.*"]
  debezium:
    # Исключаем конкретные таблицы из репликации
    table.exclude.list: "public.audit_log, public.temp_.*"
    # Настройка Heartbeat для контроля за слотом
    heartbeat.interval.ms: 5000
    # Режим старта: initial = сначала полный снимок, потом CDC
    snapshot.mode: initial

# Целевая система - StarRocks
sink:
  type: starrocks
  # Параметры подключения к StarRocks
  jdbc-url: jdbc:mysql://starrocks-fe:9030
  load-url: starrocks-fe:8030
  database-name: target_db
  username: starrocks_user
  password: starrocks_password
  # Управление загрузкой данных в StarRocks
  sink.buffer-flush.interval-ms: 10000
  sink.buffer-flush.max-rows: 50000
  sink.properties.format: json
  sink.properties.strip_outer_array: true

# Конвейер (пайплайн) - определяет, как данные текут
pipeline:
  name: Postgres to StarRocks Sync
  # Параллелизм выполнения задания во Flink
  parallelism: 2
  # Режим исполнения: streaming (потоковая обработка CDC)
  execution-mode: streaming
  # Включение автоматической синхронизации схемы (ключевая фича!)
  schema-change:
    sync-database: true
    sync-table: true
    # Политика реакции на DDL (например, ADD COLUMN):
    # - IGNORE - проигнорировать
    # - WARN - предупредить в логах
    # - APPLY (по умолчанию) - применить изменения в приемнике
    sync-ddl: APPLY
  # Простая трансформация "на лету" (опционально)
  transforms:
    - type: column-projection
      # Проецируем только нужные столбцы
      includes: "id,name,email,created_at"
    - type: add-column
      # Добавляем служебный столбец с временем обработки
      columns: "process_time TIMESTAMP"
      value: "CURRENT_TIMESTAMP"
```

### 🚀 Шаг 3: Запуск пайплайна и управление
1.  **Запуск**: Из директории Flink CDC выполните команду.
    ```bash
    ./bin/flink-cdc.sh pipeline /путь/к/postgres-to-starrocks.yaml
    ```
2.  **Что происходит при запуске** (ваша расшифровка верна):
    *   Утилита `flink-cdc.sh` парсит YAML, строит граф задания Flink (DAG) и отправляет его в кластер через REST API.
    *   Источник (PostgreSQL CDC) подключается к БД, **проверяет параметры** (`wal_level=logical`), **создает слот репликации** с указанным именем (или использует существующий) и начинает чтение.
    *   Стартует процесс: **начальный снимок (snapshot)** → **инкрементальное чтение (CDC)**.
    *   Приемник (StarRocks Connector) начинает получать данные и загружать их в целевые таблицы через механизм Stream Load.
    *   **Создается и запускается долгоживущее задание (Flink Job)**, которое теперь управляется кластером Flink.

### 📊 Шаг 4: Мониторинг и реакция на изменения
Управление и мониторинг происходят **через стандартные механизмы Flink**, а не через отдельный процесс `flink-cdc.sh`.

| Аспект | Как это работает и где смотреть |
| :--- | :--- |
| **Отслеживание процесса** | Через **веб-интерфейс Flink** (`http://<jobmanager>:8081`). Там видны все запущенные задания, их состояние, пайплайн, метрики (количество записей, задержка). |
| **Логирование** | Логи пишутся в **стандартные выходы TaskManager'ов**. Их можно смотреть в веб-интерфейсе или в файлах `log/flink-*-taskexecutor-*.out` на нодах кластера. |
| **Реакция на изменения DDL** | Если в PostgreSQL выполняется `ALTER TABLE`, Debezium улавливает это как событие. Политика `sync-ddl: APPLY` заставляет пайплайн **автоматически применить** это изменение (например, добавить столбец) в StarRocks. |
| **Управление состоянием** | Flink **периодически создает чекпоинты** (снимки состояния). Это гарантирует ровно-одноразовую обработку (exactly-once) и позволяет восстановить задание с последней контрольной точки при сбое. |
| **Остановка/перезапуск** | Чтобы остановить синхронизацию, нужно **отменить задание (Flink Job)** через веб-интерфейс или REST API. **При перезапуске с теми же `slot.name` оно продолжит с последней зафиксированной позиции LSN в слоте**. |

### ⚠️ Важные нюансы и ограничения настройки
1.  **Управление слотом**: Параметр `slot.name` в YAML позволяет задать имя, но **тонкий контроль** (например, размер слота, принудительный дроп при ошибке) через YAML невозможен. Это нужно делать вручную в PostgreSQL (`SELECT pg_drop_replication_slot('flink_cdc_slot');`).
2.  **Выбор таблиц**: Возможен через `table-list` с шаблонами и `debezium.table.exclude.list`. Однако **выбрать таблицы из разных схем или с сложной логикой** — сложно. ........................**<-- проверить**
3.  **Трансформации**: Ограничены предопределенными типами (`column-projection`, `add-column` и др.). **Сложная бизнес-логика** (оконные агрегации, JOIN) в YAML не выражается.
4.  **Публикация (Publication)**: В текущей реализации **нельзя явно указать имя существующей публикации**. Коннектор, скорее всего, создает её самостоятельно, что может быть неоптимально для вашего контроля. ..................................**<-- проверить**

### 💡 Краткий итог
**Да, Flink CDC YAML идеально подходит для цели «синхронизировать почти все таблицы с минимальными усилиями».** Он автоматизирует 90% рутины. Однако, как мы видим в параметрах, **вы жертвуете тонким контролем над репликацией PostgreSQL** (публикациями, мониторингом слота) и **гибкостью в трансформациях**.

# Настройки YAML
> где можно посмотреть полный перечень и описание возможных параметров файла YAML-конфигурации?

Официального, единого перечня всех параметров YAML для Flink CDC 3.5 пока не существует. Инструмент активно развивается, и подробная, завершённая документация пока не выпущена. Однако вы можете собрать информацию из нескольких источников.

Основные источники информации и примеры параметров для вашего сценария синхронизации PostgreSQL со StarRocks:

| Тип параметра | Что можно найти | Где искать (с примерами) | Особенности для вашего случая |
| :--- | :--- | :--- | :--- |
| **Параметры для PostgreSQL** | Официальный источник: **пакет `flink-sql-connector-postgres-cdc`**. Он же используется "под капотом" декларативного пайплайна. | **Страница проекта Flink CDC на GitHub** (например, [релиз 3.5.0](https://github.com/apache/flink-cdc/releases/tag/release-3.5.0)). В архиве `flink-cdc-3.5.0/lib/` файл **`flink-sql-connector-postgres-cdc-3.5.0.jar`** содержит встроенную документацию в `META-INF/services`. | Указывайте в секции `source:` вашего YAML (как в примерах ниже). |
| **Параметры для StarRocks** | Аналогично, смотрите документацию коннектора **`flink-connector-starrocks`**. | На [странице проекта StarRocks](https://docs.starrocks.io/docs/data-integration/flink-connector/starrocks-flink-connector/) или внутри JAR-файла. | Указывайте в секции `sink:`. Параметры будут похожи на настройки для других баз (Kafka, Paimon). |
| **Структура и примеры YAML** | **Статьи и руководства от сообщества** (Alibaba Cloud, BytePlus, Decodable) показывают реальное использование. | В результатах поиска есть примеры для MySQL → Paimon, MySQL → ByteHouse, обзор концепций. | Общая структура (source, sink, pipeline) и принципы настройки (выбор таблиц, трансформации) **применимы и к PostgreSQL**. |

### 🛠️ Как работать с настройками на практике
Вот как вы можете систематизировать поиск и применение параметров:

1.  **Получите документацию коннекторов**: Самый прямой путь — изучить `JAR`-файлы коннекторов. Например, распакуйте `flink-sql-connector-postgres-cdc-*.jar` и найдите файлы в `META-INF/services/` (часто `org.apache.flink.table.factories.Factory`), которые могут содержать список поддерживаемых параметров.

2.  **Экстраполируйте примеры для MySQL**: Посмотрите на **общую структуру** YAML-файлов для MySQL и сопоставьте её с известными параметрами из коннектора PostgreSQL. Например, структура секции `source:` для Postgres будет аналогична:
    ```yaml
    source:
      type: postgres
      name: PostgreSQL Source
      hostname: <хост>
      port: 5432
      username: <пользователь>
      password: <пароль>
      database-name: <база>
      schema-name: public
      # Предполагаемые параметры выбора таблиц (используйте те, что указаны в коннекторе)
      table-list: ["public.*"]  # для синхронизации почти всех таблиц
      # Возможные параметры репликации PostgreSQL
      slot.name: flink_cdc_slot
      debezium.heartbeat.interval.ms: 5000
    ```

3.  **Используйте практические руководства**: Обратитесь к блогам и гайдам, которые подтверждают архитектуру работы и раскрывают возможности YAML, например:
    *   **Синхронизация почти всех таблиц**: Используйте шаблоны вроде `table-list: ["public.*"]` и `debezium.table.exclude.list` для исключений.
    *   **Трансформации**: Раздел `transform:` позволяет добавлять вычисляемые столбцы, фильтровать строки и переназначать ключи.
    *   **Контроль схемы**: Раздел `pipeline.schema-change` (или `sink`-параметры вроде `sink.ignore.delete`) позволяет гибко управлять реакцией на DDL.
    *   **Запуск и мониторинг**: В статье от BytePlus есть полезные примеры поиска в логах для отслеживания стадии снапшота и инкрементальной репликации.

> **⚠️ Важно**: Официальная документация по Flink CDC 3.5 YAML пока находится в разработке. Текущие возможности лучше всего изучать через исходный код и примеры сообщества.
