# Apache Flink CDC
## Общее сравнение Flink CDC YAML (декларативные пайплайны) в версии 3.5.0+ с классическим подходом через Flink SQL
Новый режим **Flink CDC YAML (декларативные пайплайны)** в версии 3.5.0+ действительно предоставляет простой способ для синхронизации практически всех таблиц. Он более удобен, но менее гибок по сравнению с классическим подходом через Flink SQL и DataStream API.

### ⚖️ Сравнение подходов: YAML vs. Connectors
Для вашей задачи «синхронизировать почти все таблицы с минимальными усилиями» это ключевой выбор. В таблице ниже — основные отличия, собранные из анализа документации и блогов.

| Критерий | **Flink CDC YAML (декларативные пайплайны)** | **Flink CDC Connectors (SQL/DataStream API)** |
| :--- | :--- | :--- |
| **Цель и концепция** | Упрощенный, декларативный подход для описания сквозного пайплайна в одном YAML-файле. | Низкоуровневый строительный блок (источник данных) для создания любых пользовательских заданий Flink. |
| **Настройка репликации** | **Ограниченная**. Контроль через общие параметры коннектора (часто без доступа к спец. настройкам, например, слота репликации). | **Полная**. Прямой доступ ко всем параметрам Debezium (включая управление слотами, публикациями, heartbeat). |
| **Выбор таблиц** | Декларативно, через шаблоны регулярных выражений (например, `my_db\.\.*`). | Декларативно (в SQL) или программно (в DataStream), с точным контролем. |
| **Трансформация данных** | Базовая поддержка: проекции, фильтры, добавление столбцов, встроенные и UDF-функции. | Полная мощь Flink SQL или Java/Scala API для любой сложной логики. |
| **Синхронизация схемы (Schema Evolution)** | **Основное преимущество**. Встроенная автоматическая обработка и тонкий контроль (разрешить/запретить DDL). | Требует ручной реализации через слушателей событий или кастомную логику. |
| **Работа со слотом репликации** | **Неясно/ограничено**. В документации по YAML для PostgreSQL детали не раскрыты. | **Полный контроль**. Явное задание `slot.name`, `publication.name`, `heartbeat.interval.ms`. |
| **Сложность развертывания** | **Низкая**. Один YAML-файл, утилита `flink-cdc.sh` для запуска. | **Высокая**. Требует написания и сборки кода (SQL/Java), настройки Flink job. |

### ⚡ Ограничения YAML в тонкой настройке репликации
Для вашего сценария критичны именно ограничения в настройке самой репликации PostgreSQL:
*   **Управление слотом и публикацией**: В найденных примерах YAML для MySQL и PostgreSQL нет параметров вроде `slot.name` или `publication.name`. Скорее всего, коннектор создает их автоматически, лишая вас контроля над жизненным циклом и предотвращением переполнения.
*   **Фильтрация на уровне WAL**: Синхронизация «почти всех таблиц» в YAML возможна через `tables: my_schema\.\.*` и исключение через `exclude.tables`. Однако вы не сможете точечно настроить, какие таблицы попадают в одну публикацию, а это ключ для управления одним слотом.
*   **Контроль snapshot и heartbeat**: Нет прямого доступа к низкоуровневым настройкам Debezium для управления начальным снимком или heartbeat, что важно для больших БД.

### 🛠️ Развертывание Flink CDC YAML: общий план
1.  **Подготовка инфраструктуры**: Убедитесь, что есть работающий кластер Apache Flink и настроена переменная `FLINK_HOME`.
2.  **Установка Flink CDC 3.5.0+**: Скачайте дистрибутив с [официального сайта](https://downloads.apache.org/flink/flink-cdc-3.5.0/), распакуйте и скопируйте JAR-файлы пайплайн-коннекторов в директорию `lib/` вашего кластера Flink.
3.  **Создание YAML-конфигурации**: Создайте файл конфигурации, используя синтаксис, показанный в примерах выше. Для PostgreSQL в качестве источника нужно указать `type: postgres` и задать параметры подключения.
4.  **Запуск пайплайна**: Отправьте задание на выполнение с помощью утилиты командной строки: `bin/flink-cdc.sh /path/to/your-pipeline.yaml`.
5.  **Мониторинг**: Отслеживайте выполнение задания через веб-интерфейс Flink (обычно на порту 8081).

### 💎 Вывод и рекомендация для вашей задачи
Декларативный подход отлично подходит для цели «синхронизировать почти все таблицы с минимальными усилиями». Он возьмет на себя самую сложную часть — **автоматическую синхронизацию схемы**.

Однако **вам придется смириться с ограниченным контролем над процессом репликации в PostgreSQL**. Если в будущем появятся проблемы (например, рост лага или переполнение слота), диагностика и точечная настройка через YAML могут быть затруднены.

**Рекомендация**: Начните с Flink CDC YAML для быстрого старта. Если позднее потребуется тонкий контроль, вы всегда сможете переписать пайплайн, используя Flink CDC Connectors и Flink SQL, взяв за основу уже готовую логику.

## Схематичное сравнение

Ниже представлена сравнительная схема двух подходов. Flink CDC YAML — это **надстройка**, которая берет на себя функции построения пайплайна и управления схемой, делегируя работу по CDC стандартным коннекторам.

### 📊 Схема архитектуры: Flink CDC YAML vs. Flink CDC Connectors + Flink SQL
```mermaid
flowchart TD
    subgraph SRC [Источник данных]
        PG[(PostgreSQL)]
    end

    subgraph FLINK_YAML [Стек: Flink CDC 3.5 YAML Pipeline]
        A1[Конфиг-файл<br>.yaml] --> A2[Утилита<br>flink-cdc.sh]
        A2 --> A3{Сборка<br>Flink Job}
        A3 --> A4[Автоматическое создание<br>1. Источника (CDC Connector)<br>2. Приёмника<br>3. Пайплайна DDL синхронизации]
    end

    subgraph FLINK_CLASSIC [Стек: Flink CDC Connectors + Flink SQL]
        B1[SQL-скрипт /<br>Java-код] --> B2[Flink SQL Client /<br>Flink Cluster REST API]
        B2 --> B3[Создание Source Table<br>CDC Connector<br>под капотом]
        B3 --> B4[Ручное написание<br>трансформаций и<br>синхронизации схемы]
    end

    subgraph FLINK_CORE [Ядро Flink & Connectors]
        F1[Flink CDC<br>Postgres Connector]
        F2[Flink<br>JobManager]
        F3[Flink TaskManager<br>выполняет задачи]
    end

    subgraph TARGET [Целевое хранилище]
        DEST[(StarRocks/<br>Kafka/etc.)]
    end

    %% Data Flow
    PG -- CDC поток --> F1
    F1 -- DataStream --> F3
    F3 --> DEST

    %% Control & Deployment Flow
    A4 -- управляет --> F2
    B4 -- управляет --> F2
    F2 -- планирует задачи --> F3
```

### 📝 Ключевые отличия по компонентам и процессу
Архитектурная схема выше иллюстрирует следующие фундаментальные различия:

| Этап / Компонент | **Flink CDC YAML (декларативный пайплайн)** | **Flink CDC Connectors + Flink SQL (классический подход)** |
| :--- | :--- | :--- |
| **Точка входа** | Единственный файл **YAML-конфигурации**, описывающий источник, приемник и простые трансформации. | Написанный вами **SQL-скрипт** (или **Java/Scala-код**), который определяет DDL таблиц, логику ETL и управление заданиями. |
| **Пайплайн и трансформации** | **Автоматически генерируется** движком на основе декларативного описания. Вы ограничены встроенными операциями (фильтр, проекция, добавление полей). | **Полностью контролируется вами**. Вы используете всю мощь Flink SQL (окна, агрегации, JOIN) или DataStream API для создания кастомной логики. |
| **Синхронизация схемы** | **Автоматизирована**. Пайплайн сам следит за изменениями DDL в источнике и применяет их к приемнику. | **Требует ручной реализации** (например, через кастомные обработчики событий или внешние системы версионирования схем). |
| **Развертывание и запуск** | Выполняется одной утилитой: **`bin/flink-cdc.sh your-pipeline.yaml`**. Пайплайн компилируется и отправляется в кластер автоматически. | 1. Отправка SQL-скрипта через **Flink SQL Client**.<br>или<br>2. Сборка JAR-файла и отправка через **REST API** или **веб-интерфейс** Flink. |
| **Основа (ядро)** | **Использует те же самые Flink CDC Connectors** в качестве источника данных. YAML — это **слой управления** поверх них. | Прямое, низкоуровневое использование коннекторов как источника данных внутри вашего кода. |

### 🎯 Для вашей задачи «синхронизировать почти все таблицы с минимальными усилиями»
Архитектура Flink CDC YAML создана именно для таких сценариев. **Она автоматизирует самый сложный и рутинный этап — построение и поддержку пайплайна**, включая синхронизацию схемы. Это похоже на использование SMT, но на более современном и унифицированном стеке (Flink).

**Старт будет быстрым**, но важно помнить о ключевом **архитектурном компромиссе**:
*   **Вы получаете**: Быстрое развертывание и автоматизацию.
*   **Вы теряете**: Глубокий контроль над репликацией PostgreSQL (управление слотами, публикациями) и возможность создавать сложную бизнес-логику обработки.

Если этого компромисса достаточно для старта — Flink CDC YAML отличный выбор. Если же требуется тонкая настройка репликации или сложные трансформации — классический подход с SQL/API остаётся предпочтительным.

