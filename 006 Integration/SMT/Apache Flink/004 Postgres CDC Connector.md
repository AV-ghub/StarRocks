# [Postgres CDC Connector](https://nightlies.apache.org/flink/flink-cdc-docs-release-3.1/docs/connectors/flink-sources/postgres-cdc/#postgres-cdc-connector)
## Настройка Flink CDC connector
Настройки Flink CDC connector — это ключевой уровень взаимодействия с логическим декодером PostgreSQL для контроля репликации.

### 📋 Полный перечень настроек Flink CDC Connector для PostgreSQL
Вы можете найти полный и официальный перечень всех возможных опций в документации проекта Flink CDC.

| Категория | Примеры опций | Что контролирует | Особенности для вашей задачи |
| :--- | :--- | :--- | :--- |
| **Обязательные подключения** | `hostname`, `port`, `username`, `password`, `database-name`, `schema-name`, `table-name` | Базовые параметры подключения к БД. | Для синхронизации нескольких таблиц потребуется создать отдельный коннектор (или таблицу в Flink SQL) для каждой. |
| **Управление репликацией** | `slot.name`, `decoding.plugin.name`, `publication.name`, `heartbeat.interval.ms` | **Слоты, плагины и публикации** — основа тонкого контроля. | **Ключевая опция**. Явно задавайте `slot.name` и `publication.name` для управления слотом на уровне публикации. |
| **Стратегия чтения (Snapshot)** | `scan.startup.mode`, `scan.incremental.snapshot.enabled`, `scan.incremental.snapshot.chunk.size` | **Поведение начального снапшота** и инкрементального чтения. | `scan.incremental.snapshot.enabled=true` — современный режим с чанками и чекапоинтами. |
| **Паузы и границы** | `scan.incremental.snapshot.chunk.key-column` | Ключ для разбиения большой таблицы на чанки. | Позволяет задать колонку для равномерного распределения нагрузки при снапшоте. |
| **Сквозные настройки Debezium** | `debezium.*` (например, `debezium.snapshot.mode`) | **Низкоуровневые параметры движка Debezium**. | Мощный инструмент для тонкой настройки. Например, `'debezium.snapshot.select.statement.overrides.schema.table'` позволяет задать кастомный SQL для выборки данных. |

### 🎯 Как использовать настройки для вашего сценария
Ваша цель — **один слот на публикацию и синхронизация десятка таблиц**. Вот пошаговый подход:

1.  **На стороне PostgreSQL**: Создайте одну публикацию для всех нужных таблиц.
    ```sql
    CREATE PUBLICATION my_custom_pub FOR TABLE users, orders, products ...;
    ```

2.  **На стороне Flink SQL**: Для каждой синхронизируемой таблицы создайте источник (`CREATE TABLE ... WITH (...)`). **Важно**: в параметрах каждого источника укажите **одинаковые** `slot.name` и `publication.name`.
    ```sql
    -- Источник для таблицы 'users'
    CREATE TABLE flink_users_source (
      -- схема таблицы
    ) WITH (
      'connector' = 'postgres-cdc',
      'hostname' = '...',
      'slot.name' = 'my_flink_slot',           -- ОДИН И ТОТ ЖЕ слот
      'publication.name' = 'my_custom_pub',    -- ОДНА И ТА ЖЕ публикация
      'table-name' = 'users',
      'decoding.plugin.name' = 'pgoutput',
      'scan.incremental.snapshot.enabled' = 'true' -- Используем улучшенный снапшот
    );
    -- Аналогично создаем источник для orders, products и т.д.
    ```

3.  **Контроль роста слота**: Настройте `heartbeat.interval.ms` (например, `'heartbeat.interval.ms' = '10000'`). Это заставит коннектор периодически отправлять служебные события, даже если в ваших таблицах нет изменений, не давая слоту "застаиваться" и бесконечно накапливать WAL.

### 🔧 Мониторинг и работа со слотами
После запуска следите за слотами через PostgreSQL:
```sql
-- Проверьте лаг репликации
SELECT slot_name,
       pg_size_pretty(pg_wal_lsn_diff(pg_current_wal_lsn(), confirmed_flush_lsn)) AS replication_lag
FROM pg_replication_slots
WHERE slot_name = 'my_flink_slot';
```

### 💡 Вывод
**Flink CDC Connector** действительно предоставляет **полный контроль** над логической репликацией PostgreSQL через свои настройки.

Ключевые принципы для вашего случая:
*   **Один слот на публикацию**: Задайте одинаковые `slot.name` и `publication.name` во всех коннекторах.
*   **Безопасный снапшот**: Используйте инкрементальный снапшот (`scan.incremental.snapshot.enabled=true`).
*   **Защита от роста**: Включите heartbeat (`heartbeat.interval.ms`).
*   **Тонкая настройка**: При необходимости используйте префикс `debezium.*`.

Поскольку Flink CDC активно развивается, для получения самой актуальной информации всегда сверяйтесь с [официальной документацией](https://nightlies.apache.org/flink/flink-cdc-docs-release-3.1/docs/connectors/flink-sources/postgres-cdc/).

## Настройка Flink CDC connector в SMT
**В SMT вы не можете напрямую указать параметры Flink CDC connector для PostgreSQL (такие как `slot.name` или `heartbeat.interval.ms`)**. Эти настройки необходимо добавить в сгенерированный SQL-скрипт Flink уже после работы SMT.

### 📝 Как это работает: Два этапа работы SMT
Чтобы понять, где можно настраивать, важно помнить, что SMT делает две вещи:

| Этап | Что делает SMT | Пример вывода | Настройки Flink CDC |
| :--- | :--- | :--- | :--- |
| **1. Создание DDL для StarRocks** | Генерирует SQL для создания таблиц в StarRocks (`starrocks-create.*.sql`) | `CREATE TABLE users ...` | ❌ Неприменимо |
| **2. Генерация пайплайна Flink** | Генерирует SQL для задания Flink (`flink-create.*.sql`) | `CREATE TABLE users_source WITH (...)` | ✅ **Именно здесь** |

Согласно документации SMT, её основной задачей является чтение схемы исходной базы данных и **автоматическая генерация готовых SQL-скриптов** для Flink, предназначенных для последующего ручного запуска пользователем. Она **не вносит эти настройки напрямую в свой конфигурационный файл `config_prod.conf`**.

### 🛠️ Пошаговый план для тонкой настройки
Чтобы реализовать ваши требования (один слот, десяток таблиц, контроль роста), действуйте так:

1.  **Настройте SMT для выбора таблиц**
    В секции `[table-rule.1]` файла `config_prod.conf` используйте `database` и `table` с регулярными выражениями, чтобы выбрать нужные вам таблицы. Это создаст для них Flink-задание.
    ```ini
    [table-rule.1]
    database = ^my_db$
    table = ^(users|orders|products|cart)$ # Пример выбора конкретных таблиц
    ```

2.  **Запустите SMT** для генерации скриптов (`flink-create.*.sql`).

3.  **Вручную отредактируйте сгенерированный SQL-скрипт Flink**
    Откройте файл `result/flink-create.1.sql`. Для каждой созданной таблицы-источника (`CREATE TABLE ..._source`) в блоке `WITH (...)` добавьте нужные параметры Flink CDC.

    ```sql
    -- Пример фрагмента сгенерированного и изменённого скрипта
    CREATE TABLE users_source (
        -- ... столбцы ...
    ) WITH (
      'connector' = 'postgres-cdc',
      'hostname' = '...',
      'database-name' = 'my_db',
      'table-name' = 'users',
      -- Добавленные вручную настройки для контроля репликации:
      'slot.name' = 'my_custom_slot',         -- ОДИН общий слот для всех таблиц
      'publication.name' = 'my_custom_pub',   -- Ваша публикация в PG
      'heartbeat.interval.ms' = '10000',      -- Контроль роста слота
      'scan.startup.mode' = 'initial',        -- Стратегия старта
      'debezium.snapshot.mode' = 'initial'    -- Доп. настройки через debezium.*
    );
    ```
    **Ключевой момент**: Укажите **одинаковые** `slot.name` и `publication.name` для всех таблиц-источников в одном задании.

### 💡 Важное замечание о SMT
Обратите внимание, что **SMT официально считается устаревшим (deprecated) инструментом** с января 2023 года. Разработчики StarRocks рекомендуют вместо него использовать современный **Apache Flink CDC 3.0** и выше, который обладает более богатыми возможностями настройки и управления, включая синхронизацию схемы.

Для вашего сценария **тонкой настройки репликации PostgreSQL подход на основе чистого Flink CDC (как мы обсуждали ранее) будет прямее и даст вам полный контроль** без промежуточных инструментов вроде SMT.
