Разберём подробно инкрементальный снапшот (incremental snapshot) во Flink CDC — это ключевая техника для минимизации влияния на источник.

Инкрементальный снапшот **практически не блокирует таблицы** и позволяет читать данные параллельными чанками, что даёт огромные преимущества в производительности и снижает нагрузку на источник.

### 🔍 Как работает инкрементальный снапшот

Вместо того чтобы считывать всю таблицу одним запросом (как в классическом Debezium `snapshot.mode=initial`), инкрементальный снапшот разбивает процесс на умные этапы. На схеме ниже показан весь цикл:

```mermaid
flowchart LR
    subgraph S1[Этап 1: Подготовка и чтение чанков]
        direction LR
        A1[Определение границ<br>через PK] --> A2[Разбивка на чанки]
        A2 --> A3[Параллельное чтение<br>чанков (SELECT)]
    end

    subgraph S2[Этап 2: Совмещение с CDC]
        B1[Захват новых изменений<br>из WAL (UPDATE/DELETE)] --> B2[Корректировка данных<br>в чанках (отслеживание)]
    end

    subgraph S3[Этап 3: Завершение]
        C1[Все чанки обработаны] --> C2[Плавный переход<br>на чистый CDC-поток]
    end

    S1 --> S2 --> S3
    S2 -.->|Корректирует| S1
```

**Ключевые моменты процесса**:
*   **Параллельное чтение**: Разные части таблицы (чанки) читаются одновременно несколькими задачами Flink, что ускоряет процесс.
*   **Отслеживание изменений**: Пока идёт чтение снапшота, коннектор **уже слушает поток изменений (CDC)** из WAL. Если какая-то только что прочитанная строка изменилась, коннектор применяет это изменение к уже считанным данным, обеспечивая консистентность.
*   **Минимальные блокировки**: Для определения границ чанков может использоваться кратковременная блокировка (например, `SHARE UPDATE EXCLUSIVE` в PG), но **само чтение данных не требует долгих блокировок таблицы**.

### 🆚 Сравнение стратегий снапшота
Чтобы было нагляднее, сравним две основные стратегии:

| Характеристика | **Классический (полный) снапшот** (`initial`) | **Инкрементальный снапшот** (`incremental`) |
| :--- | :--- | :--- |
| **Блокировка таблицы** | **Долгая** (на время всего SELECT). Может быть `ACCESS SHARE`, но блокирует конкурирующие DDL (ALTER TABLE). | **Кратковременная или отсутствует**. Только для определения границ чанков. |
| **Влияние на источник** | **Высокое**. Длительный SELECT нагружает CPU/память/диск, может мешать основной работе. | **Низкое**. Нагрузка распределена во времени и может быть ограничена параллелизмом. |
| **Консистентность** | **Моментальный снимок** на время начала SELECT. Изменения во время чтения игнорируются. | **Консистентность на уровне чанка с коррекцией**. Учитывает изменения, произошедшие во время чтения. |
| **Производительность** | Медленная для больших таблиц, однопоточное чтение. | **Быстрая**, благодаря параллельному чтению чанков. |
| **Настройка во Flink SQL** | `'debezium.snapshot.mode' = 'initial'` | `'scan.incremental.snapshot.enabled' = 'true'` (и параметры чанков) |

### ⚙️ Как настроить инкрементальный снапшот во Flink SQL
Вот пример конфигурации источника PostgreSQL CDC с инкрементальным снапшотом:

```sql
CREATE TABLE pg_source (...)
WITH (
    'connector' = 'postgres-cdc',
    'hostname' = '...',
    -- ... остальные параметры подключения ...
    'slot.name' = 'flink_cdc_slot',

    -- КЛЮЧЕВЫЕ ПАРАМЕТРЫ ИНКРЕМЕНТАЛЬНОГО СНАПШОТА:
    'scan.incremental.snapshot.enabled' = 'true',     -- Включаем технику
    'scan.incremental.snapshot.chunk.size' = '8096',  -- Кол-во строк в чанке
    'scan.incremental.snapshot.chunk.key-column' = 'constantid', -- Колонка для разбивки (обычно PK)

    -- Параметры Debezium для снапшота (все ещё важны):
    'debezium.snapshot.mode' = 'initial', -- или 'never', если таблица пустая
    'debezium.heartbeat.interval.ms' = '10000'
);
```

### 💡 Что это даёт в вашем сценарии
1.  **Минимизация простоя**: Вы можете начать синхронизацию **десятков больших таблиц**, не опасаясь "положить" продакшен-базу долгими блокировками.
2.  **Контроль над нагрузкой**: Настройкой `chunk.size` и параллелизма задания можно регулировать нагрузку на PostgreSQL.
3.  **Надёжность**: Технология проверена на очень больших таблицах (сотни миллионов строк) в продакшене.

**Итог**: Инкрементальный снапшот — это не просто оптимизация, а **принципиально иной, более безопасный и производительный подход** к начальной синхронизации. Для вашей задачи синхронизации многих таблиц он практически обязателен.

Если вы решите продолжить с Flink SQL, настройка этих параметров станет следующим логичным шагом после очистки `lib/` от старых JAR-файлов и подготовки двух правильных коннекторов (Postgres CDC и StarRocks).
