## Monitoring

Пердустановка

```bash
sudo apt install htop
sudo apt install sysstat
```

### 🔍 Проверка загрузки CPU и памяти

Вот несколько команд для проверки загрузки процессора:

1.  **`top` или `htop`** (лучше, если установлен):
    ```
    top
    ```
    *   В первой строке вы увидите общую загрузку CPU (`%Cpu(s)`).
    *   Нажмите `1`, чтобы развернуть загрузку по ядрам.
    *   Сортируйте процессы по потреблению CPU: `Shift + P` (в `top`).

2.  **`mpstat`** (показывает детальную статистику):
    ```
    mpstat 1 5  # Каждую секунду, 5 раз
    ```
    Смотрите столбец `%idle` (простой). Если он постоянно ниже 10-20%, CPU перегружен.

3.  **Краткий вывод `vmstat`**:
    ```
    vmstat 1 5
    ```
    Первые два столбца после `procs` — `r` (процессы, ожидающие CPU) и `b` (спящие процессы). Если `r` постоянно больше числа CPU-ядер — есть очередь и нехватка CPU.

### Проверка смежных "пожирателей" памяти

```bash
# Сортировка процессов по использованию памяти
ps aux --sort=-%mem | head -20
```

### Оптимизация Flink

`/.../flink-xxx/conf/config.yaml`


```yaml
# УМЕНЬШИТЕ ПАМЯТЬ НА КАЖДЫЙ TASK MANAGER
taskmanager.memory.process.size: 1024m  # 1 ГБ вместо стандартных 2-4ГБ

# УМЕНЬШИТЕ КОЛИЧЕСТВО СЛОТОВ НА ОДНОМ TM
taskmanager.numberOfTaskSlots: 1  # Один слот на TM при параллелизме=1

# ОПТИМИЗИРУЙТЕ УПРАВЛЕНИЕ ПАМЯТЬЮ
taskmanager.memory.managed.fraction: 0.3  # Доля управляемой памяти (можно уменьшить)
```

### SQL-клиент Flink

```sql
-- 1. СНИЗЬТЕ ПАРАЛЛЕЛИЗМ ДО МИНИМУМА
Flink SQL> SET 'parallelism.default' = '1';  -- Самое важное! Запустит все операторы в одном потоке

-- 2. УМЕНЬШИТЕ ОБЪЁМ СОХРАНЯЕМОГО СОСТОЯНИЯ (если используете окна/аггрегаты)
Flink SQL> SET 'table.exec.state.ttl' = '60000';  -- Время жизни состояния в мс (1 минута вместо дефолтных 30 дней)

-- 3. ПЕРЕЗАПУСТИТЕ ЗАДАНИЕ
-- Остановите текущий job через Web UI или командой, затем выполните ваш INSERT-запрос заново
```

**Внимание**: После изменения `/.../flink-xxx/conf/config.yaml` нужно перезапустить кластер Flink.

### 🚀 Краткий план действий

1.  **Сразу же** в SQL-клиенте Flink выполните `SET 'parallelism.default' = '1';` и перезапустите задание.
2.  Проверьте `top` или `htop` — если загрузка CPU всё ещё близка к 100%, задание будет работать медленно, но хотя бы не "упадёт" из-за нехватки памяти.
3.  Если задание запустилось — отлично. Затем можно постепенно увеличивать `parallelism.default` до `2`, наблюдая за `free` и `top`.
4.  Если памяти всё равно мало — настройте `/.../flink-xxx/conf/config.yaml` или рассмотрите возможность увеличения оперативной памяти на сервере.






## Resources
##### [Configuring Flink application resources](https://docs.cloudera.com/csa/1.13.4/how-to-flink/topics/csa-other-configs.html)
